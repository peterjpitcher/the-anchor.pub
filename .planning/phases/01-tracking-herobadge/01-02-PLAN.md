---
phase: 01-tracking-herobadge
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/drinks/page.tsx
  - app/feltham-pub/page.tsx
  - app/private-hire/wakes/page.tsx
  - app/private-hire/page.tsx
  - app/function-room-hire/page.tsx
  - app/karaoke/page.tsx
  - app/our-pub/page.tsx
  - app/egham-pub/page.tsx
  - app/christmas-parties/christmas-hero-ctas.tsx
  - app/christmas-parties/client-components.tsx
  - app/corporate-events/page.tsx
  - app/restaurants-near-heathrow/page.tsx
  - app/food-menu/vegan/page.tsx
  - app/food-menu/gluten-free/page.tsx
  - app/food-menu/vegetarian/page.tsx
  - app/food-menu/page.tsx
  - app/book-table/page.tsx
  - app/about/page.tsx
  - app/heathrow-parking/[terminal]/page.tsx
  - app/heathrow-parking/confirmation/[bookingId]/page.tsx
  - app/heathrow-parking/page.tsx
  - app/quiz-night/page.tsx
  - app/bedfont-pub/page.tsx
  - app/beer-garden/page.tsx
  - app/music-bingo/page.tsx
  - app/cash-bingo/page.tsx
  - app/safety-and-respect/page.tsx
  - app/live-music/page.tsx
  - app/near-heathrow/terminal-4/page.tsx
  - app/near-heathrow/terminal-2/page.tsx
  - app/parking/bookings/[id]/page.tsx
  - app/accessibility/page.tsx
  - app/page.tsx
  - app/stanwell-pub/page.tsx
  - app/whats-on/page.tsx
  - app/dog-friendly-pub-heathrow/page.tsx
  - app/sunday-lunch/page.tsx
  - lib/error-handling.ts
  - lib/promos/privateHire2026.ts
autonomous: true
requirements: [TRACK-01, COMP-01]

must_haves:
  truths:
    - "Every clickable phone number on the site fires a GTM phone_call_click event"
    - "No raw tel: href links exist outside PhoneLink or PhoneButton components"
    - "All hero badge markup across pages uses the HeroBadge component"
    - "No inline hardcoded rating/review badge markup remains on any page"
    - "Phone source names follow page_location convention (e.g. home_hero, contact_footer)"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with HeroBadge and tracked phone links"
      contains: "HeroBadge"
    - path: "app/book-table/page.tsx"
      provides: "Booking page with tracked phone link"
      contains: "PhoneLink"
  key_links:
    - from: "app/*/page.tsx"
      to: "components/PhoneLink.tsx"
      via: "import and render"
      pattern: "import.*PhoneLink"
    - from: "app/*/page.tsx"
      to: "components/HeroBadge.tsx"
      via: "import and render"
      pattern: "import.*HeroBadge"
---

<objective>
Migrate all raw tel: links to PhoneLink/PhoneButton and replace inline badge markup with HeroBadge.

Purpose: TRACK-01 requires every phone link to fire GTM events. COMP-01 requires all pages to use the HeroBadge component instead of inline badge markup. After this plan, no raw tel: hrefs or hardcoded badge markup remain anywhere in the codebase.

Output: 35+ page files updated, zero raw tel: links remaining, HeroBadge deployed site-wide.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tracking-herobadge/01-01-SUMMARY.md

<interfaces>
<!-- Components available after Plan 01 -->

From components/PhoneLink.tsx (updated in Plan 01):
```typescript
interface PhoneLinkProps {
  phone: string           // Display format: '01753 682707'
  source: string          // GTM source: 'page_location' format (per D-07)
  className?: string
  children?: React.ReactNode
  showIcon?: boolean      // Defaults to true (SVG phone icon)
  onClick?: () => void
  role?: string
}
// 'use client' component — fires trackPhoneCallClick on click
```

From components/PhoneButton.tsx (unchanged):
```typescript
interface PhoneButtonProps {
  phone: string
  source: string
  variant?: 'primary' | 'secondary' | 'outline'
  size?: 'sm' | 'md' | 'lg'
  className?: string
  children?: React.ReactNode
}
// 'use client' component — wraps Button, fires trackPhoneCallClick
```

From components/HeroBadge.tsx (rebuilt in Plan 01):
```typescript
interface HeroBadgeProps {
  className?: string
}
// Server Component — renders Badge primitives with SSOT.json data
// Shows: "{rating}/5 on Google" + "{review_count} reviews"
export function HeroBadge({ className }: HeroBadgeProps): JSX.Element
```

From lib/constants.ts:
```typescript
export const CONTACT = {
  phone: '01753 682707',
  phoneHref: 'tel:+441753682707',
  ...
}
export const PHONE_NUMBER = CONTACT.phone  // Added in Plan 01
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all raw tel: links to PhoneLink/PhoneButton</name>
  <files>
    app/drinks/page.tsx,
    app/feltham-pub/page.tsx,
    app/private-hire/wakes/page.tsx,
    app/private-hire/page.tsx,
    app/function-room-hire/page.tsx,
    app/karaoke/page.tsx,
    app/our-pub/page.tsx,
    app/egham-pub/page.tsx,
    app/christmas-parties/christmas-hero-ctas.tsx,
    app/christmas-parties/client-components.tsx,
    app/corporate-events/page.tsx,
    app/restaurants-near-heathrow/page.tsx,
    app/food-menu/vegan/page.tsx,
    app/food-menu/gluten-free/page.tsx,
    app/food-menu/vegetarian/page.tsx,
    app/food-menu/page.tsx,
    app/book-table/page.tsx,
    app/about/page.tsx,
    app/heathrow-parking/[terminal]/page.tsx,
    app/heathrow-parking/confirmation/[bookingId]/page.tsx,
    app/heathrow-parking/page.tsx,
    app/quiz-night/page.tsx,
    app/bedfont-pub/page.tsx,
    app/beer-garden/page.tsx,
    app/music-bingo/page.tsx,
    app/cash-bingo/page.tsx,
    app/safety-and-respect/page.tsx,
    app/live-music/page.tsx,
    app/near-heathrow/terminal-4/page.tsx,
    app/near-heathrow/terminal-2/page.tsx,
    app/parking/bookings/[id]/page.tsx,
    app/accessibility/page.tsx,
    lib/error-handling.ts,
    lib/promos/privateHire2026.ts
  </files>
  <read_first>
    - components/PhoneLink.tsx (updated API with SVG icon)
    - components/PhoneButton.tsx (unchanged API)
    - lib/constants.ts (CONTACT.phone, PHONE_NUMBER)
    - Each page file being modified (read before editing to see current tel: usage and context)
  </read_first>
  <action>
For every file listed, find ALL raw `tel:` href links and replace them with the appropriate tracked component. Per D-06, use context to choose:

**Use PhoneLink** (inline text links):
- `<a href="tel:+441753682707" className="...">01753 682707</a>` becomes `<PhoneLink phone={CONTACT.phone} source="PAGE_LOCATION" className="..." showIcon={false} />`
- When the phone number appears mid-sentence (e.g. "Call us on 01753 682707 to discuss"), use `showIcon={false}` since the icon would break the sentence flow.
- When it is a standalone link or at the end of a line, keep `showIcon` default (true).

**Use PhoneButton** (button/CTA contexts):
- `<Link href="tel:+441753682707"><Button ...>Call Us</Button></Link>` becomes `<PhoneButton phone={CONTACT.phone} source="PAGE_LOCATION" variant="..." size="...">Call Us</PhoneButton>`

**Source naming convention (per D-07):** `page_location` format. Examples:
- `app/drinks/page.tsx` CTA section: `"drinks_cta"`
- `app/beer-garden/page.tsx` hero: `"beer-garden_hero"`
- `app/private-hire/wakes/page.tsx` body text: `"wakes_body"`
- `app/food-menu/page.tsx` kitchen notice: `"food-menu_kitchen-notice"`
- `app/book-table/page.tsx` fallback: `"book-table_fallback"`
- `app/heathrow-parking/page.tsx` CTA: `"heathrow-parking_cta"`, FAQ: `"heathrow-parking_faq"`, terms: `"heathrow-parking_terms"`

**Phone number centralisation (per D-08):**
- Import `{ CONTACT }` from `@/lib/constants` and use `CONTACT.phone` instead of hardcoded `'01753 682707'` or `'+441753682707'` strings.
- For files that are Server Components (no `'use client'`), you will need to wrap the PhoneLink in a small client boundary. If the page already has a `'use client'` directive, just add the import. If the page is a Server Component, either:
  (a) Extract the phone link section into a small client component in the same file, or
  (b) Import PhoneLink — since PhoneLink already has `'use client'`, it can be used inside a Server Component as a leaf client component.

**For non-component files:**
- `lib/error-handling.ts` line 166: Replace `phoneLink: 'tel:+441753682707'` with `phoneLink: CONTACT.phoneHref` (import CONTACT from constants).
- `lib/promos/privateHire2026.ts` line 21: Replace `'tel:+441753682707'` with `CONTACT.phoneHref` (import CONTACT from constants).
- `app/christmas-parties/christmas-hero-ctas.tsx` line 18: Replace `'tel:+441753682707'` with `CONTACT.phoneHref`.
- `app/christmas-parties/client-components.tsx` line 22: Replace `'tel:+441753682707'` with `CONTACT.phoneHref`.

**Important:** PhoneLink is a client component, so importing it inside server component pages is fine (React renders it on the client). No need to add 'use client' to page files.

After ALL migrations, run a final grep to confirm zero raw tel: links remain:
```bash
grep -rn 'href="tel:' app/ components/ lib/ --include="*.tsx" --include="*.ts" | grep -v 'PhoneLink\|PhoneButton\|phoneHref\|CONTACT'
```
This should return empty.
  </action>
  <verify>
    <automated>grep -rn 'href="tel:' app/ lib/ --include="*.tsx" --include="*.ts" | grep -v 'PhoneLink\|PhoneButton\|phoneHref\|CONTACT\|node_modules' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'href="tel:' app/ lib/ --include="*.tsx" --include="*.ts" | grep -v 'PhoneLink\|PhoneButton\|phoneHref\|CONTACT'` returns 0 lines
    - `grep -rn 'PhoneLink\|PhoneButton' app/ --include="*.tsx" | wc -l` returns 30+ (at least one per page that had a raw tel link)
    - `grep -rn "CONTACT.phone" app/ --include="*.tsx" | head -5` returns results (phone number centralised)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All 35+ raw tel: links across the site replaced with PhoneLink or PhoneButton. Every clickable phone number now fires GTM tracking. Phone numbers reference CONTACT constant, not hardcoded strings.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline hero badge markup with HeroBadge component</name>
  <files>
    app/page.tsx,
    app/drinks/page.tsx,
    app/feltham-pub/page.tsx,
    app/private-hire/page.tsx,
    app/function-room-hire/page.tsx,
    app/karaoke/page.tsx,
    app/stanwell-pub/page.tsx,
    app/whats-on/page.tsx,
    app/food-menu/page.tsx,
    app/book-table/page.tsx,
    app/sunday-lunch/page.tsx,
    app/beer-garden/page.tsx
  </files>
  <read_first>
    - components/HeroBadge.tsx (rebuilt API from Plan 01 — className prop only)
    - Each page file being modified (read before editing to see current inline badge markup)
  </read_first>
  <action>
Replace all inline hero badge/rating markup with the HeroBadge component. The inline patterns to find and replace:

**Pattern 1 — Inline badge span:**
```tsx
<span className="inline-flex items-center gap-1.5 bg-white/10 border border-white/25 rounded-full px-3 py-1 text-xs font-medium text-white/90 backdrop-blur-sm">Rated 4.6/5 on Google</span>
```
Replace with: `<HeroBadge />`

**Pattern 2 — Paragraph with star emojis:**
```tsx
<p className="text-center text-sm text-anchor-cream-text/70">... <strong>Rated 4.6/5 on Google</strong> ... </p>
```
Replace with: `<HeroBadge className="text-sm" />`

**Pattern 3 — Paragraph without emojis:**
```tsx
<p className="text-center text-sm text-anchor-cream-text/70"><strong className="text-anchor-cream-text">Rated 4.6/5 on Google</strong> ... </p>
```
Replace with: `<HeroBadge className="text-sm" />`

**Pattern 4 — List item:**
```tsx
<li>4.6/5 on Google</li>
```
Replace with: `<li><HeroBadge /></li>` (or restructure the surrounding list as appropriate)

**Pages and their patterns:**
- `app/page.tsx` (~line 172): Pattern 2 paragraph → `<HeroBadge />`
- `app/drinks/page.tsx` (~line 245): Pattern 1 span → `<HeroBadge />`
- `app/feltham-pub/page.tsx` (~line 113): Pattern 2 paragraph → `<HeroBadge />`
- `app/private-hire/page.tsx` (~line 189): Pattern 2 with stars → `<HeroBadge />`
- `app/function-room-hire/page.tsx` (~line 149): Pattern 3 paragraph → `<HeroBadge />`
- `app/karaoke/page.tsx` (~line 307): Pattern 3 paragraph → `<HeroBadge />`
- `app/stanwell-pub/page.tsx` (~line 122): Pattern 2 with stars → `<HeroBadge />`
- `app/whats-on/page.tsx` (~line 197): Pattern 3 paragraph → `<HeroBadge />`
- `app/food-menu/page.tsx` (~line 283): Pattern 1 span → `<HeroBadge />`
- `app/book-table/page.tsx` (~line 202): Pattern 1 span → `<HeroBadge />`
- `app/sunday-lunch/page.tsx` (~line 225): Pattern 4 list item → `<HeroBadge />`

Note: The `<HeroBadge />` component is a Server Component, so it can be used directly in any page without adding `'use client'`.

Import statement to add at the top of each file:
```tsx
import { HeroBadge } from '@/components/HeroBadge'
```

After ALL replacements, run a grep to confirm no inline badge markup remains:
```bash
grep -rn 'Rated 4.6/5 on Google' app/ --include="*.tsx" | grep -v 'metadata\|description\|answer:'
```
This should return empty (metadata descriptions are NOT replaced — only visible rendered markup).
  </action>
  <verify>
    <automated>grep -rn 'Rated 4.6/5 on Google' app/ --include="*.tsx" | grep -v 'metadata\|description\|answer:\|alt\|title' | wc -l | tr -d ' ' && grep -rn 'import.*HeroBadge' app/ --include="*.tsx" | wc -l | tr -d ' ' && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'Rated 4.6/5 on Google' app/ --include="*.tsx" | grep -v 'metadata\|description\|answer:\|alt\|title'` returns 0 lines (no inline rendered badge markup)
    - `grep -rn 'import.*HeroBadge' app/ --include="*.tsx" | wc -l` returns 10+ (HeroBadge imported in at least 10 page files)
    - `grep -rn 'bg-white/10 border border-white/25 rounded-full' app/ --include="*.tsx" | wc -l` returns 0 (old badge span styling removed)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All inline hero badge markup across 10+ pages replaced with HeroBadge component. Rating and review count are now sourced from SSOT.json via the component. Updating SSOT.json updates all pages automatically.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'href="tel:' app/ lib/ --include="*.tsx" --include="*.ts" | grep -v 'PhoneLink\|PhoneButton\|phoneHref\|CONTACT'` returns 0 lines
2. `grep -rn 'Rated 4.6/5 on Google' app/ --include="*.tsx" | grep -v 'metadata\|description\|answer:\|alt\|title'` returns 0 lines
3. `grep -rn 'import.*PhoneLink\|import.*PhoneButton' app/ --include="*.tsx" | wc -l` returns 30+
4. `grep -rn 'import.*HeroBadge' app/ --include="*.tsx" | wc -l` returns 10+
5. `npm run build` completes without errors
6. `npm run lint` passes
</verification>

<success_criteria>
- Zero raw tel: href links remain outside PhoneLink/PhoneButton/CONTACT.phoneHref
- Every clickable phone number fires a GTM tracking event
- All inline hero badge markup replaced with HeroBadge component
- Source names follow page_location convention
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-tracking-herobadge/01-02-SUMMARY.md`
</output>
