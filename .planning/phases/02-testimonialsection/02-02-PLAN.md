---
phase: 02-testimonialsection
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/private-hire/page.tsx
  - app/private-hire/baby-showers/page.tsx
  - app/private-hire/christenings/page.tsx
  - app/private-hire/wakes/page.tsx
  - app/private-hire/gender-reveal/page.tsx
  - app/christmas-parties/client-components.tsx
  - app/book-table/page.tsx
  - app/near-heathrow/page.tsx
autonomous: true
requirements: [COMP-06]

must_haves:
  truths:
    - "All ad-hoc testimonial/review markup across existing pages is replaced with TestimonialSection"
    - "No pages contain hand-rolled review markup outside the TestimonialSection component"
    - "No raw star entity strings (&#9733;) remain in the 8 target page files"
    - "Existing page layout and visual appearance is preserved (same quotes, same authors)"
  artifacts:
    - path: "app/private-hire/page.tsx"
      provides: "Full-variant testimonial section replacing 4-card grid"
      contains: "TestimonialSection"
    - path: "app/book-table/page.tsx"
      provides: "Pull-quote variant replacing inline centred quote"
      contains: "pull-quote"
    - path: "app/christmas-parties/client-components.tsx"
      provides: "Full-variant replacing TESTIMONIALS.map block"
      contains: "TestimonialSection"
  key_links:
    - from: "app/private-hire/page.tsx"
      to: "components/TestimonialSection.tsx"
      via: "import TestimonialSection"
      pattern: "from.*TestimonialSection"
    - from: "app/book-table/page.tsx"
      to: "components/TestimonialSection.tsx"
      via: "import TestimonialSection"
      pattern: "from.*TestimonialSection"
    - from: "app/christmas-parties/client-components.tsx"
      to: "components/TestimonialSection.tsx"
      via: "import TestimonialSection"
      pattern: "from.*TestimonialSection"
---

<objective>
Replace all 8 pages of hand-rolled testimonial markup with TestimonialSection component calls.

Purpose: Eliminates ad-hoc testimonial markup site-wide, completing the TestimonialSection standardisation (COMP-06).
Output: 8 page files updated, zero raw star entities or hand-rolled quote blocks remaining.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-testimonialsection/02-RESEARCH.md
@.planning/phases/02-testimonialsection/02-UI-SPEC.md
@.planning/phases/02-testimonialsection/02-01-SUMMARY.md

<interfaces>
<!-- TestimonialSection API created by Plan 01 -->

From components/TestimonialSection.tsx:
```typescript
export interface Testimonial {
  quote: string      // Raw text, no surrounding quotes
  author: string     // Display name
  source?: string    // e.g. "Google Review"
  rating?: number    // 1-5, defaults to 5
}

export interface TestimonialSectionProps {
  variant?: 'full' | 'compact' | 'pull-quote'
  reviews: Testimonial[]
  title?: string       // full variant only, default: "What Our Guests Say"
  subtitle?: string    // full variant only, default: "From Google Reviews"
  className?: string
}

export function TestimonialSection(props: TestimonialSectionProps): JSX.Element | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace testimonial markup on private-hire pages and book-table</name>
  <files>
    app/private-hire/page.tsx
    app/private-hire/baby-showers/page.tsx
    app/private-hire/christenings/page.tsx
    app/private-hire/wakes/page.tsx
    app/private-hire/gender-reveal/page.tsx
    app/book-table/page.tsx
  </files>
  <read_first>
    - components/TestimonialSection.tsx
    - app/private-hire/page.tsx
    - app/private-hire/baby-showers/page.tsx
    - app/private-hire/christenings/page.tsx
    - app/private-hire/wakes/page.tsx
    - app/private-hire/gender-reveal/page.tsx
    - app/book-table/page.tsx
  </read_first>
  <action>
    Add `import { TestimonialSection } from '@/components/TestimonialSection'` to each file. Then replace hand-rolled testimonial blocks with component calls. Preserve the EXACT same quote text, author names, and sources from the existing markup.

    **app/private-hire/page.tsx** — Replace the 4-card grid block (around lines 547-585) that has `SectionHeader title="What Our Guests Say"` and 4 hand-rolled divs with `&#9733;` stars. Replace with:
    ```tsx
    <TestimonialSection
      variant="full"
      title="What Our Guests Say"
      subtitle="From Google Reviews"
      className="section-spacing bg-anchor-bg-card border-b border-anchor-gold/15"
      reviews={[
        { quote: "[exact quote from line ~559]", author: "[exact author]", source: "Google Review", rating: 5 },
        { quote: "[exact quote from line ~566]", author: "[exact author]", source: "Google Review", rating: 5 },
        { quote: "[exact quote from line ~573]", author: "[exact author]", source: "Google Review", rating: 5 },
        { quote: "[exact quote from line ~580]", author: "[exact author]", source: "Google Review", rating: 5 },
      ]}
    />
    ```
    Remove the entire hand-rolled section including the `<section>`, `<Container>`, `<SectionHeader>`, and the grid div. The TestimonialSection handles its own internal layout.

    **app/private-hire/baby-showers/page.tsx** — Replace the 2 inline italic quotes (around lines 314-318). Extract the quote text (strip `&ldquo;`/`&rdquo;` — component adds them). Replace with:
    ```tsx
    <TestimonialSection
      variant="compact"
      reviews={[
        { quote: "[exact text without ldquo/rdquo]", author: "Baby Shower Guest", source: "Google Review", rating: 5 },
        { quote: "[exact text without ldquo/rdquo]", author: "Baby Shower Guest", source: "Google Review", rating: 5 },
      ]}
    />
    ```
    Preserve the surrounding section structure if it contains other content. Only replace the `<p className="... italic ...">` quote elements.

    **app/private-hire/christenings/page.tsx** — Replace the 2 inline italic quotes (around lines 370-374). Same pattern as baby-showers:
    ```tsx
    <TestimonialSection
      variant="compact"
      reviews={[
        { quote: "[exact text without ldquo/rdquo]", author: "Christening Guest", source: "Google Review", rating: 5 },
        { quote: "[exact text without ldquo/rdquo]", author: "Christening Guest", source: "Google Review", rating: 5 },
      ]}
    />
    ```

    **app/private-hire/wakes/page.tsx** — Replace the 3 inline italic quotes (around lines 338-346). Same pattern:
    ```tsx
    <TestimonialSection
      variant="compact"
      reviews={[
        { quote: "[exact text without ldquo/rdquo]", author: "Wake Guest", source: "Google Review", rating: 5 },
        { quote: "[exact text without ldquo/rdquo]", author: "Wake Guest", source: "Google Review", rating: 5 },
        { quote: "[exact text without ldquo/rdquo]", author: "Wake Guest", source: "Google Review", rating: 5 },
      ]}
    />
    ```

    **app/private-hire/gender-reveal/page.tsx** — Replace the 2 inline italic quotes (around lines 318-322). Same pattern:
    ```tsx
    <TestimonialSection
      variant="compact"
      reviews={[
        { quote: "[exact text without ldquo/rdquo]", author: "Gender Reveal Guest", source: "Google Review", rating: 5 },
        { quote: "[exact text without ldquo/rdquo]", author: "Gender Reveal Guest", source: "Google Review", rating: 5 },
      ]}
    />
    ```

    **app/book-table/page.tsx** — Replace the single centred pull-quote block (around line 406). The existing block is inside a Section with `spacing="md"` and has `<p className="text-2xl text-anchor-cream-text/80 italic leading-relaxed">`. Replace with:
    ```tsx
    <TestimonialSection
      variant="pull-quote"
      className="bg-anchor-bg-raised border-b border-anchor-gold/15"
      reviews={[{
        quote: "Lovely pub, great food, friendly staff. We stopped in on our way to Heathrow and wished we'd found it sooner. Will definitely be back.",
        author: "Anonymous",
        source: "Google Review",
        rating: 5
      }]}
    />
    ```
    Remove the old `<Section>` wrapper and the `<div className="max-w-2xl mx-auto text-center">` and `<p>` elements — TestimonialSection handles its own layout.

    **CRITICAL:** Copy the EXACT quote text from each page. Do NOT paraphrase or shorten. Strip only the `&ldquo;`/`&rdquo;` entities and `&apos;` (replace with `'`). The component adds curly quotes automatically.
  </action>
  <verify>
    <automated>grep -c "&#9733;" app/private-hire/page.tsx && echo "FAIL: star entities remain" || echo "PASS: no star entities"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TestimonialSection" app/private-hire/page.tsx` returns at least 1
    - `grep -c "TestimonialSection" app/private-hire/baby-showers/page.tsx` returns at least 1
    - `grep -c "TestimonialSection" app/private-hire/christenings/page.tsx` returns at least 1
    - `grep -c "TestimonialSection" app/private-hire/wakes/page.tsx` returns at least 1
    - `grep -c "TestimonialSection" app/private-hire/gender-reveal/page.tsx` returns at least 1
    - `grep -c "TestimonialSection" app/book-table/page.tsx` returns at least 1
    - `grep -r "&#9733;" app/private-hire/page.tsx` returns no matches
    - `grep "pull-quote" app/book-table/page.tsx` returns a match
    - `grep "from.*TestimonialSection" app/private-hire/page.tsx` returns a match (import present)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    6 page files updated: private-hire (1 full-variant, 4 compact-variant subpages) and book-table (1 pull-quote). All hand-rolled quote markup replaced with TestimonialSection calls. Original quote text preserved exactly. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace testimonial markup on christmas-parties and near-heathrow pages</name>
  <files>
    app/christmas-parties/client-components.tsx
    app/near-heathrow/page.tsx
  </files>
  <read_first>
    - components/TestimonialSection.tsx
    - app/christmas-parties/client-components.tsx
    - app/near-heathrow/page.tsx
  </read_first>
  <action>
    **app/christmas-parties/client-components.tsx** — This file has `'use client'` at the top. TestimonialSection is a Server Component with no server-only APIs (no async, no server-only imports), so it CAN be imported and rendered inside a client component file without error.

    Replace the `TESTIMONIALS` array (around lines 255-271) and the `.map()` rendering block that creates Card-based testimonial cards. Add import:
    ```tsx
    import { TestimonialSection } from '@/components/TestimonialSection'
    ```

    Replace the TESTIMONIALS.map rendering with:
    ```tsx
    <TestimonialSection
      variant="full"
      title="What Our Guests Say"
      subtitle="From Previous Christmas Parties"
      reviews={[
        {
          quote: "We've booked our works Christmas do at The Anchor three years running. The food is brilliant, the staff remember our names, and nobody has to fight for parking. Best Christmas party pub near Heathrow, hands down.",
          author: "Sarah T.",
          source: "Office manager, Poyle business park",
          rating: 5
        },
        {
          quote: "Organised a Christmas buffet for 40 crew from different terminals. Everyone found the place easily, the buffet was generous, and the bar tab system meant zero fuss on the night. Already rebooked.",
          author: "James R.",
          source: "Airline operations team, Heathrow",
          rating: 5
        },
        {
          quote: "We booked a Christmas lunch for twelve, the turkey was honestly better than my nan's. Don't tell her. Crackers, candles, the lot. Proper festive without being tacky.",
          author: "Michelle K.",
          source: "Staines-upon-Thames",
          rating: 5
        }
      ]}
    />
    ```

    Remove the `const TESTIMONIALS = [...]` array declaration since the data is now inline in the component call. If any other code references the TESTIMONIALS constant, move the data inline to that reference too.

    Verify the build succeeds — this is the client component boundary risk identified in research. If a build error occurs about Server Component imports, confirm TestimonialSection has NO `'use server'` directive and no server-only imports (it should not).

    **app/near-heathrow/page.tsx** — Replace the single inline italic quote (a `<p className="... italic ...">` inside a centred section). Add import and replace with:
    ```tsx
    <TestimonialSection
      variant="pull-quote"
      reviews={[{
        quote: "[exact text from the existing italic paragraph — strip ldquo/rdquo]",
        author: "Guest",
        source: "Google Review",
        rating: 5
      }]}
    />
    ```
    Read the file to find the exact quote text and preserve it verbatim.

    **Post-replacement verification:** Run `grep -r "&#9733;" app/ --include="*.tsx"` to confirm NO raw star entity strings remain in any of the 8 target files. The only acceptable `&#9733;` usage is in `app/reviews/page.tsx` (which uses a proper StarRating component, NOT ad-hoc markup — it is not a target for replacement).
  </action>
  <verify>
    <automated>npm run build && grep -r "&#9733;" app/private-hire/ app/christmas-parties/client-components.tsx app/book-table/page.tsx app/near-heathrow/page.tsx --include="*.tsx" && echo "FAIL: star entities remain" || echo "PASS: all clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TestimonialSection" app/christmas-parties/client-components.tsx` returns at least 1
    - `grep -c "TestimonialSection" app/near-heathrow/page.tsx` returns at least 1
    - `grep "from.*TestimonialSection" app/christmas-parties/client-components.tsx` returns a match
    - `grep "TESTIMONIALS" app/christmas-parties/client-components.tsx` returns NO matches (array removed)
    - `grep "pull-quote" app/near-heathrow/page.tsx` returns a match
    - `grep -r "&#9733;" app/private-hire/ app/christmas-parties/client-components.tsx app/book-table/page.tsx app/near-heathrow/page.tsx --include="*.tsx"` returns NO matches
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    All 8 target pages use TestimonialSection. Zero raw star entity strings remain in target files. The christmas-parties client component imports and renders TestimonialSection without build errors. Lint and build both pass.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npm run lint` passes with zero warnings
- `npm test` passes all existing + new tests
- `grep -r "&#9733;" app/ --include="*.tsx"` returns matches ONLY in `app/reviews/page.tsx` (acceptable — uses proper StarRating, not ad-hoc)
- `grep -rn "TestimonialSection" app/ --include="*.tsx" | wc -l` returns at least 8 (imports across all target files)
- `grep -rn "italic.*&ldquo;" app/private-hire/ app/book-table/page.tsx app/near-heathrow/page.tsx --include="*.tsx"` returns 0 (all hand-rolled quotes removed)
</verification>

<success_criteria>
All 8 pages that previously had hand-rolled testimonial markup now use TestimonialSection with the correct variant. No raw star entities or inline italic quote blocks remain in target files. Build, lint, and tests all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testimonialsection/02-02-SUMMARY.md`
</output>
