---
phase: 03-final-standardisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/reviews/page.tsx
  - app/food-menu/page.tsx
  - app/sunday-lunch/page.tsx
  - app/book-table/page.tsx
  - app/drinks/page.tsx
  - app/about/page.tsx
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03, CONV-04, CONV-05]

must_haves:
  truths:
    - "Terminal subpages (T2-T5) each have a CTASection at bottom of page (already true)"
    - "Reviews page has a CTASection prompting visitors to book"
    - "All CTASections use consistent primary/white button variant pairing"
    - "food-menu, sunday-lunch, book-table pages have enableSmartCtas={true}"
  artifacts:
    - path: "app/reviews/page.tsx"
      provides: "CTASection import and usage at page bottom"
      contains: "CTASection"
    - path: "app/food-menu/page.tsx"
      provides: "enableSmartCtas on HeroWrapper"
      contains: "enableSmartCtas={true}"
    - path: "app/sunday-lunch/page.tsx"
      provides: "enableSmartCtas on HeroWrapper"
      contains: "enableSmartCtas={true}"
    - path: "app/book-table/page.tsx"
      provides: "enableSmartCtas on HeroWrapper"
      contains: "enableSmartCtas={true}"
  key_links:
    - from: "app/reviews/page.tsx"
      to: "components/CTASection.tsx"
      via: "import and render"
      pattern: "import.*CTASection"
---

<objective>
Add CTASection to the reviews page, enable SmartCTAs on service pages, and verify/standardise button variant pairing across all existing CTASections.

Purpose: Close CTA coverage gaps so every customer-facing page drives conversion. CONV-01 is already satisfied (terminal pages have CTASection); this plan verifies that and addresses the remaining CONV requirements.
Output: Reviews page has booking CTA, service pages show smart CTAs, consistent button variants site-wide.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- CTASection API from components/CTASection.tsx -->
```typescript
interface CTAButton {
  text: string
  href: string
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger' | 'warning' | 'white'
  size?: 'xs' | 'sm' | 'md' | 'lg'
  isPhone?: boolean
  phoneSource?: string
  isDirections?: boolean
  directionsSource?: string
  bookingContext?: string
}

interface CTASectionProps {
  title: string
  description?: string
  buttons: CTAButton[]
  variant?: 'green' | 'red' | 'dark'
  className?: string
  footer?: string
  children?: React.ReactNode
}
```

<!-- HeroWrapper enableSmartCtas prop from components/hero/HeroWrapper.tsx -->
```typescript
// Line 96: enableSmartCtas?: boolean (default false)
// Line 259: const shouldUseSmartCtas = enableSmartCtas && !hasAnyCTAOverride
```

<!-- Standard CTA button pairing pattern (from well-formed existing pages): -->
<!-- Button 1: booking URL (auto BookTableButton) or variant: "primary" -->
<!-- Button 2: variant: "white" (renders as secondary inside CTASection) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CTASection to reviews page and enable SmartCTAs on service pages</name>
  <files>app/reviews/page.tsx, app/food-menu/page.tsx, app/sunday-lunch/page.tsx, app/book-table/page.tsx</files>
  <read_first>
    - app/reviews/page.tsx (see current page structure, find where to add CTASection)
    - app/food-menu/page.tsx (find HeroWrapper to add enableSmartCtas)
    - app/sunday-lunch/page.tsx (find HeroWrapper to add enableSmartCtas)
    - app/book-table/page.tsx (find HeroWrapper to add enableSmartCtas)
    - components/CTASection.tsx (confirm import path and API)
    - app/near-heathrow/terminal-2/page.tsx (reference implementation for CTASection usage pattern)
  </read_first>
  <action>
1. **reviews/page.tsx (CONV-02):** Add `import { CTASection } from '@/components/CTASection'` at top. Before the closing tag of the main content wrapper, add a CTASection with:
   - `title="Ready to Visit?"` (or similar booking-oriented heading matching the page's tone)
   - `variant="green"` (standard for booking CTAs)
   - `buttons` array with two entries:
     - Button 1: `{ text: "Book a Table", href: "https://ordertab.menu/theanchor/bookings", bookingContext: "reviews" }` (auto BookTableButton)
     - Button 2: `{ text: "Call Us", href: "tel:+441753682707", variant: "white", isPhone: true, phoneSource: "reviews_cta" }`

2. **food-menu/page.tsx (CONV-05):** Find the `<HeroWrapper` JSX and add `enableSmartCtas={true}` prop. Do NOT change any other props.

3. **sunday-lunch/page.tsx (CONV-05):** Find the `<HeroWrapper` JSX and add `enableSmartCtas={true}` prop. Do NOT change any other props.

4. **book-table/page.tsx (CONV-05):** Find the `<HeroWrapper` JSX and add `enableSmartCtas={true}` prop. Do NOT change any other props.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-The-Anchor.pub/.claude/worktrees/admiring-dhawan-3da8c8 && grep -n "CTASection" app/reviews/page.tsx && grep -n "enableSmartCtas" app/food-menu/page.tsx app/sunday-lunch/page.tsx app/book-table/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*CTASection" app/reviews/page.tsx returns a match
    - grep "CTASection" app/reviews/page.tsx returns at least 2 matches (import + usage)
    - grep "enableSmartCtas={true}" app/food-menu/page.tsx returns exactly 1 match
    - grep "enableSmartCtas={true}" app/sunday-lunch/page.tsx returns exactly 1 match
    - grep "enableSmartCtas={true}" app/book-table/page.tsx returns exactly 1 match
    - npm run build exits 0
  </acceptance_criteria>
  <done>Reviews page has a green CTASection with booking + call buttons. food-menu, sunday-lunch, book-table pages all have enableSmartCtas={true} on HeroWrapper. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CONV-01 and audit CONV-03 button variant consistency</name>
  <files>app/reviews/page.tsx, app/drinks/page.tsx, app/about/page.tsx</files>
  <read_first>
    - app/near-heathrow/terminal-2/page.tsx (confirm CTASection exists for CONV-01)
    - app/near-heathrow/terminal-3/page.tsx (confirm CTASection exists for CONV-01)
    - app/near-heathrow/terminal-4/page.tsx (confirm CTASection exists for CONV-01)
    - app/near-heathrow/terminal-5/page.tsx (confirm CTASection exists for CONV-01)
  </read_first>
  <action>
1. **CONV-01 verification:** Confirm all four terminal pages (near-heathrow/terminal-{2,3,4,5}/page.tsx) have `<CTASection` in their source. This is already true per research. Document the finding.

2. **CONV-03 audit:** Run `grep -rn "variant.*secondary\|variant.*outline" app/ --include="*.tsx" | grep -i "cta"` to find any CTASection button arrays using `variant: "secondary"` instead of `variant: "white"`. The canonical secondary pattern inside CTASection is `variant: "white"` (which CTASection maps to the secondary visual style on coloured backgrounds). If any pages use `variant: "secondary"` directly in a CTASection buttons array, change them to `variant: "white"` for consistency. Do NOT change variant usage outside of CTASection button arrays.

3. **CONV-04 verification:** Run `grep -rn "enableSmartCtas" app/pub-near-*/page.tsx app/heathrow-hotels-pub/page.tsx app/near-heathrow/page.tsx` to confirm all location/hotel pages already have it. The research says they do. Document the finding.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-The-Anchor.pub/.claude/worktrees/admiring-dhawan-3da8c8 && echo "=== CONV-01 ===" && for t in 2 3 4 5; do grep -c "CTASection" app/near-heathrow/terminal-$t/page.tsx; done && echo "=== CONV-03 ===" && grep -rn 'variant.*"secondary"' app/ --include="*.tsx" | grep -i "buttons\|cta" | grep -v node_modules || echo "No secondary variants in CTA buttons" && echo "=== CONV-04 ===" && grep -rn "enableSmartCtas" app/pub-near-*/page.tsx | wc -l && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Each of near-heathrow/terminal-{2,3,4,5}/page.tsx returns count >= 1 for "CTASection"
    - No CTASection buttons arrays contain variant: "secondary" (all use "white" for secondary CTA)
    - All pub-near-* pages show enableSmartCtas (count >= 11)
    - npm run lint exits 0
  </acceptance_criteria>
  <done>CONV-01 verified (all terminal pages have CTASection). CONV-03 satisfied (all CTASection button variants use consistent primary/white pairing). CONV-04 verified (all location/hotel pages have enableSmartCtas). Lint passes.</done>
</task>

</tasks>

<verification>
- `grep -rn "CTASection" app/reviews/page.tsx` shows import and usage
- `grep -rn "enableSmartCtas={true}" app/food-menu/page.tsx app/sunday-lunch/page.tsx app/book-table/page.tsx` shows 3 matches
- `grep -c "CTASection" app/near-heathrow/terminal-{2,3,4,5}/page.tsx` all >= 1
- `npm run build && npm run lint` both pass
</verification>

<success_criteria>
- Reviews page has CTASection with booking + phone buttons (CONV-02)
- 3 service pages have enableSmartCtas={true} (CONV-05)
- Terminal pages confirmed to have CTASection (CONV-01)
- Location/hotel pages confirmed to have enableSmartCtas (CONV-04)
- All CTASection button arrays use white variant for secondary (CONV-03)
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-final-standardisation/03-01-SUMMARY.md`
</output>
