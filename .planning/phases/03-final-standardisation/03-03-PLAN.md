---
phase: 03-final-standardisation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/stanwell-pub/page.tsx
  - app/live-sport/six-nations/page.tsx
  - app/about/page.tsx
  - app/m25-junction-14-pub/page.tsx
  - app/page.tsx
  - app/book-table/page.tsx
autonomous: true
requirements: [DS-01, DS-02, DS-03, DATA-01]

must_haves:
  truths:
    - "Three spacing utility classes exist in globals.css: section-spacing (py-10 md:py-12), section-spacing-sm (py-8 md:py-10), section-spacing-lg (py-12 md:py-14 lg:py-16)"
    - "Pages that previously showed hardcoded hours text now use BusinessHours component or generic phrasing"
    - "Updating hours in the management app is reflected on pages without a code change"
  artifacts:
    - path: "app/globals.css"
      provides: "Spacing token definitions matching REQUIREMENTS.md values"
      contains: "section-spacing"
    - path: "app/stanwell-pub/page.tsx"
      provides: "BusinessHours replacing hardcoded kitchen schedule"
      contains: "BusinessHours"
    - path: "app/live-sport/six-nations/page.tsx"
      provides: "BusinessHours replacing hardcoded hours"
      contains: "BusinessHours"
    - path: "app/about/page.tsx"
      provides: "Generic phrasing replacing hardcoded day references"
    - path: "app/m25-junction-14-pub/page.tsx"
      provides: "Generic phrasing replacing hardcoded day references"
    - path: "app/page.tsx"
      provides: "FAQ schema hours replaced with generic phrasing"
    - path: "app/book-table/page.tsx"
      provides: "FAQ schema hours replaced with generic phrasing"
  key_links:
    - from: "app/stanwell-pub/page.tsx"
      to: "components/BusinessHours.tsx"
      via: "import and render"
      pattern: "import.*BusinessHours"
    - from: "app/live-sport/six-nations/page.tsx"
      to: "components/BusinessHours.tsx"
      via: "import and render"
      pattern: "import.*BusinessHours"
---

<objective>
Verify spacing tokens exist in globals.css and replace all hardcoded kitchen hours strings with BusinessHours component or dynamic-safe phrasing.

Purpose: DS-01/02/03 confirm the design system tokens already exist (they do -- accept current deployed values). DATA-01 removes static hours text so updates in the management app propagate automatically.
Output: Spacing tokens verified. 6 pages updated to remove hardcoded hours.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- BusinessHours from components/BusinessHours.tsx -->
```typescript
interface BusinessHoursProps {
  showKitchen?: boolean  // default true
  className?: string
}
export function BusinessHours({ showKitchen, className }: BusinessHoursProps): JSX.Element
// Client component -- reads from BusinessHoursProvider context (already at app root)
```

<!-- Current spacing tokens in app/globals.css lines 58-77 -->
```css
.section-spacing     { @apply py-10 md:py-12; }
.section-spacing-sm  { @apply py-8 md:py-10; }
.section-spacing-md  { @apply py-10 md:py-12 lg:py-14; }
.section-spacing-lg  { @apply py-12 md:py-14 lg:py-16; }
.section-spacing-tight { @apply py-6 md:py-8; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify spacing tokens and document DS-01/02/03 as satisfied</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (lines 50-85 -- see exact spacing token definitions)
  </read_first>
  <action>
1. **Verify DS-01:** Confirm `section-spacing` class exists in globals.css with value `py-10 md:py-12` (matching REQUIREMENTS.md).

2. **Verify DS-02:** Confirm `section-spacing-sm` class exists in globals.css with value `py-8 md:py-10` (matching REQUIREMENTS.md).

3. **Verify DS-03:** Confirm `section-spacing-lg` class exists in globals.css with value `py-12 md:py-14 lg:py-16` (matching REQUIREMENTS.md).

4. **No file changes needed** for this task. The tokens exist with the correct values. Document the verification in the summary.

Note: Do NOT change the spacing token values. They are already in use across the site and match the requirements spec.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-The-Anchor.pub/.claude/worktrees/admiring-dhawan-3da8c8 && grep -n "section-spacing" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "\.section-spacing " app/globals.css returns exactly 1 match containing "py-10 md:py-12"
    - grep "\.section-spacing-sm" app/globals.css returns exactly 1 match containing "py-8 md:py-10"
    - grep "\.section-spacing-lg" app/globals.css returns exactly 1 match containing "py-12 md:py-14 lg:py-16"
  </acceptance_criteria>
  <done>DS-01, DS-02, DS-03 verified: all three spacing utility classes exist in globals.css with values matching REQUIREMENTS.md (py-10 md:py-12, py-8 md:py-10, py-12 md:py-14 lg:py-16).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hours strings with BusinessHours component</name>
  <files>app/stanwell-pub/page.tsx, app/live-sport/six-nations/page.tsx, app/about/page.tsx, app/m25-junction-14-pub/page.tsx, app/page.tsx, app/book-table/page.tsx</files>
  <read_first>
    - app/stanwell-pub/page.tsx (line ~301 -- hardcoded "Kitchen: Tue-Fri 6-9pm, Sat 1-7pm, Sun 1-6pm")
    - app/live-sport/six-nations/page.tsx (line ~205 -- hardcoded "Tue-Fri 6-9pm, Sat 1-7pm, Sun 1-6pm")
    - app/about/page.tsx (line ~243 -- hardcoded "served Tuesday to Sunday")
    - app/m25-junction-14-pub/page.tsx (line ~134 -- hardcoded "served Tuesday to Sunday")
    - app/page.tsx (full file -- find ALL instances of hardcoded hours in FAQ answer fields and visible content)
    - app/book-table/page.tsx (full file -- find ALL instances of hardcoded hours in FAQ answer fields and visible content)
    - components/BusinessHours.tsx (confirm import path and component API)
  </read_first>
  <action>
For each page, apply the appropriate fix:

1. **stanwell-pub/page.tsx** (line ~301): Replace `<p className="mt-3 text-sm text-anchor-gold">Kitchen: Tue-Fri 6-9pm, Sat 1-7pm, Sun 1-6pm</p>` with a BusinessHours component. Add `import { BusinessHours } from '@/components/BusinessHours'` if not already imported. Replace the hardcoded `<p>` with `<div className="mt-3 text-sm text-anchor-gold"><BusinessHours /></div>`.

2. **live-sport/six-nations/page.tsx** (line ~205): Replace the hardcoded `<strong>Normal Hours:</strong> Tue-Fri 6-9pm, Sat 1-7pm, Sun 1-6pm` with `<strong>Normal Hours:</strong>` followed by `<BusinessHours />`. Add the import at top.

3. **about/page.tsx** (line ~245): The text "fair prices, served Tuesday to Sunday" is editorial copy. Replace with "fair prices, with food served daily (check our opening hours for times)" or similar generic phrasing that won't go stale. Do NOT inject BusinessHours component into mid-sentence prose -- use static generic phrasing instead.

4. **m25-junction-14-pub/page.tsx** (line ~134): Replace "Burgers, pizzas and pub classics served Tuesday to Sunday" with "Burgers, pizzas and pub classics served daily (check our opening hours for times)" or similar generic phrasing. Same rationale as #3 -- mid-sentence prose, not a standalone hours display.

5. **page.tsx** (homepage): Read the full file and find all hardcoded hours references. For FAQ `answer` fields (JSON-LD schema strings where React components cannot render), replace specific hours text (e.g. "Tue-Fri 6-9pm, Sat 1-7pm, Sun 1-6pm" or "Tuesday to Sunday" or similar day/time references) with generic phrasing like "We serve food daily -- visit our website for current opening hours." For any visible JSX content with standalone hours displays, use `<BusinessHours />` component instead.

6. **book-table/page.tsx** (line ~462): Read the full file and find all hardcoded hours references. For FAQ `answer` fields (JSON-LD schema strings), replace specific hours text with generic phrasing like "Check our website for current food service hours." For any visible JSX content with standalone hours displays, use `<BusinessHours />` component instead.

**Key rule:** Use `<BusinessHours />` for standalone hours display blocks. Use generic phrasing for mid-sentence prose or FAQ schema `answer` fields where a React component cannot be rendered.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-The-Anchor.pub/.claude/worktrees/admiring-dhawan-3da8c8 && echo "=== Hardcoded hours check (specific times) ===" && grep -rn "Tue-Fri 6-9pm\|Tue-Fri 6–9pm\|Tue–Fri 6–9pm\|Sat 1-7pm\|Sat 1–7pm" app/stanwell-pub/page.tsx app/live-sport/six-nations/page.tsx app/page.tsx app/book-table/page.tsx && echo "FAIL: hardcoded times found" || echo "PASS: no hardcoded times" && echo "=== Static day references ===" && grep -rn "served Tuesday to Sunday\|Tuesday to Sunday" app/about/page.tsx app/m25-junction-14-pub/page.tsx app/page.tsx app/book-table/page.tsx && echo "FAIL: static day refs found" || echo "PASS: no static day refs" && echo "=== Build ===" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep "Tue-Fri 6-9pm\|Tue–Fri 6–9pm" app/stanwell-pub/page.tsx returns 0 matches
    - grep "Tue-Fri 6-9pm\|Tue–Fri 6–9pm" app/live-sport/six-nations/page.tsx returns 0 matches
    - grep "Tue-Fri 6-9pm\|Tue–Fri 6–9pm\|Sat 1-7pm\|Sat 1–7pm" app/page.tsx returns 0 matches
    - grep "Tue-Fri 6-9pm\|Tue–Fri 6–9pm\|Sat 1-7pm\|Sat 1–7pm" app/book-table/page.tsx returns 0 matches
    - grep "served Tuesday to Sunday\|Tuesday to Sunday" app/about/page.tsx returns 0 matches
    - grep "served Tuesday to Sunday\|Tuesday to Sunday" app/m25-junction-14-pub/page.tsx returns 0 matches
    - grep "served Tuesday to Sunday\|Tuesday to Sunday" app/page.tsx returns 0 matches
    - grep "served Tuesday to Sunday\|Tuesday to Sunday" app/book-table/page.tsx returns 0 matches
    - grep "BusinessHours" app/stanwell-pub/page.tsx returns at least 1 match
    - grep "BusinessHours" app/live-sport/six-nations/page.tsx returns at least 1 match
    - npm run build exits 0
  </acceptance_criteria>
  <done>All hardcoded kitchen schedule strings removed from all 6 target pages including homepage and book-table. Standalone hours displays use BusinessHours component. Mid-sentence prose and FAQ schema text uses generic phrasing. Build passes.</done>
</task>

</tasks>

<verification>
- `grep -n "section-spacing" app/globals.css` shows all three tokens with correct values
- `grep -rn "Tue-Fri 6-9pm\|Tue–Fri 6–9pm\|served Tuesday to Sunday" app/stanwell-pub/page.tsx app/live-sport/six-nations/page.tsx app/about/page.tsx app/m25-junction-14-pub/page.tsx app/page.tsx app/book-table/page.tsx` returns 0 matches
- `grep "BusinessHours" app/stanwell-pub/page.tsx app/live-sport/six-nations/page.tsx` returns matches
- `npm run build && npm run lint` both pass
</verification>

<success_criteria>
- DS-01/02/03: Spacing tokens verified as existing in globals.css with values matching REQUIREMENTS.md
- DATA-01: No hardcoded kitchen schedule strings remain in any of the 6 target pages (stanwell-pub, six-nations, about, m25-junction-14-pub, homepage, book-table)
- Pages with standalone hours display use BusinessHours component
- Pages with mid-sentence hours references or FAQ schema text use generic phrasing
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-final-standardisation/03-03-SUMMARY.md`
</output>
