// Deprecated location. The canonical Input now lives in
// `components/ui/primitives/Input.tsx` (redesign spec §4.4). This thin
// re-export keeps `@/components/ui/forms` consumers working during migration.
// Scheduled for deletion in Phase 6.
export { Input } from '../primitives/Input'
export type { InputProps } from '../primitives/Input'
