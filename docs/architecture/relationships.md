---
generated: true
last_updated: 2026-04-28T00:00:00Z
source: session-setup
project: the-anchor-pub
---

# Relationships

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

Cross-reference map linking routes, API handlers, integrations, and environment variables.

## Integration map

### Management API (`management.orangejelly.co.uk`)

The primary external dependency. Every data read and write flows through it.

| Env var | `ANCHOR_API_KEY`, `ANCHOR_API_BASE_URL` |
|---|---|
| Auth | `Authorization: Bearer ${ANCHOR_API_KEY}` |
| Client | `lib/api/client.ts` -> `anchorAPI.*` methods |

| Route / Page | API handler | Management endpoint |
|---|---|---|
| StatusBar (global) | `/api/business/hours` | `GET /business/hours` |
| `/book-table` | `/api/table-bookings/availability` | `GET /table-bookings/availability` |
| `/book-table` (submit) | `/api/table-bookings` | `POST /table-bookings` |
| `/book-table` (cancel) | `/api/table-bookings/[reference]` DELETE | `DELETE /table-bookings/[reference]` |
| `/book-table` (Sunday lunch menu) | `/api/table-bookings/menu/sunday-lunch` | `GET /menus` |
| `/whats-on`, `/events/*`, sitemap | `/api/events` | `GET /events` |
| Event booking | `/api/event-bookings` | `POST /event-bookings` |
| Event waitlist | `/api/event-waitlist` | `POST /event-waitlist` |
| Event categories | `/api/event-categories` | `GET /event-categories` |
| `/drinks/managers-special` | `/api/managers-special` | `GET /managers-special` |
| `/reviews` | `/api/reviews` | `GET /reviews` |
| `/heathrow-parking/*` | `/api/parking/availability`, `/api/parking/rates` | `GET /parking/*` |
| Parking booking | `/api/parking/bookings` | `POST /parking/bookings` |
| Private hire forms | `/api/public/private-booking` | `POST /private-booking` |
| Customer lookup | `/api/customers/lookup` | `GET /customers` |
| Christmas enquiry | `/api/enquiry/christmas` | `POST /enquiries/christmas` |
| Open mic enquiry | `/api/enquiry/open-mic` | `POST /enquiries/open-mic` |
| Calendar | `/api/calendar/upcoming`, `/api/calendar/event/[id]` | `GET /calendar/*` |

---

### PayPal

Inline payment checkout for table bookings and parking.

| Env var | `NEXT_PUBLIC_PAYPAL_CLIENT_ID` |
|---|---|
| Client-side SDK | `@paypal/react-paypal-js` |
| Order creation | `/api/table-bookings/paypal/create-order`, `/api/parking/payment/create-order` |
| Order capture | `/api/table-bookings/paypal/capture-order`, `/api/parking/payment/capture` |
| Payment return | `/api/booking/payment-return` |

---

### Cloudflare Turnstile

Anti-spam / bot protection on public forms.

| Env vars | `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (client), `TURNSTILE_SECRET_KEY` (server) |
|---|---|
| Client widget | `@marsidev/react-turnstile` |
| Server verification | `lib/turnstile.ts` |
| Used on | Table booking form, private hire enquiry forms, event booking form |

---

### Google Tag Manager

Client-side analytics event tracking.

| Env var | `NEXT_PUBLIC_GTM_ID` (defaults to `GTM-WWFQTQS`) |
|---|---|
| Provider | `components/tracking/GTMProvider.tsx` |
| Event helpers | `lib/gtm-events.ts`, `lib/gtm-events/menu-events.ts` |
| Layout injection | `app/layout.tsx` |
| Used on | All pages — conversion events for bookings, enquiries, CTAs |

---

### Microsoft Clarity

Session recording and heatmaps.

| Env var | `NEXT_PUBLIC_CLARITY_PROJECT_ID` |
|---|---|
| Hook | `lib/use-clarity.ts` |
| Injected in | `app/layout.tsx` |

---

### AviationStack

Live Heathrow flight data for parking availability pages.

| Env var | `NEXT_PUBLIC_AVIATIONSTACK_API_KEY` |
|---|---|
| Client | `lib/flights.ts` |
| Used on | `/heathrow-parking/*` pages |

---

### Microsoft Graph (Azure AD)

Email delivery for Christmas party enquiries.

| Env vars | `MICROSOFT_TENANT_ID`, `MICROSOFT_CLIENT_ID`, `MICROSOFT_CLIENT_SECRET`, `MICROSOFT_USER_EMAIL`, `CHRISTMAS_ENQUIRY_TO` |
|---|---|
| Used in | `app/api/enquiry/christmas/route.ts` only |
| Purpose | Sends enquiry confirmation emails via Office 365 mailbox |

---

## Dependency graph (text)

```
Browser
  +-- GTM / Clarity (client-side analytics)
  +-- PayPal SDK (payment UI)
  +-- Turnstile widget (bot protection)
  +-- fetch('/api/*')
       +-- Next.js API route handlers (server-side proxy)
            |-- management.orangejelly.co.uk  [ANCHOR_API_KEY]
            |    +-- Supabase DB (in management app)
            |-- PayPal API  (order create / capture)
            |-- Turnstile verify API  [TURNSTILE_SECRET_KEY]
            |-- AviationStack API  [NEXT_PUBLIC_AVIATIONSTACK_API_KEY]
            +-- Microsoft Graph API  [MICROSOFT_*]
```

## Key files

| File | Role |
|---|---|
| `lib/api/client.ts` | Management API client (`anchorAPI.*`) + availability fallback logic |
| `lib/api/index.ts` | Barrel re-export of every domain helper |
| `lib/api.ts` | Legacy shim — `export * from './api/index'` |
| `lib/management-api-base.ts` | API base URL resolution |
| `lib/hours-utils.ts` | Business hours parsing — use `??` not `\|\|` for kitchen null checks |
| `lib/table-booking-service-windows.ts` | Converts hours into bookable time slots |
| `lib/booking-helpers.ts` | Booking availability helpers with build-time guards |
| `lib/sunday-lunch-cutoff.ts` | 1pm Saturday Europe/London cutoff |
| `lib/constants.ts` | Sunday lunch deposit constants + brand |
| `lib/gtm-events.ts` | GTM dataLayer push helpers |
| `lib/turnstile.ts` | Server-side Turnstile token verification |
| `lib/flights.ts` | AviationStack flight data client |
| `lib/seasonal-utils.ts` | Seasonal image resolution |
| `lib/error-handling.ts` | User-friendly error message mapping |
| `middleware.ts` | Domain/HTTPS normalisation + cache/security headers |
| `app/sitemap.ts` | Pulls events and landmarks from management API for XML sitemap |

## Sunday-lunch surface (high-density)

| Layer | File(s) |
|---|---|
| Pages | `app/sunday-lunch/page.tsx`, `app/book-table/page.tsx` |
| API proxies | `app/api/table-bookings/menu/sunday-lunch/route.ts`, `app/api/table-bookings/paypal/create-order/route.ts`, `…/capture-order/route.ts`, `app/api/table-bookings/route.ts`, `app/api/booking/submit/route.ts`, `app/api/booking/agent/route.ts` |
| Wizard | `components/features/BookingWizard/{index,WizardStep2SundayOffer,WizardStep2bMenuSelection,WizardStep5DetailsAndRequirements,WizardStep7Confirm,types}.tsx` |
| Forms / sections | `components/features/TableBooking/{SundayLunchBooking,SundayLunchBookingForm,SundayLunchBookingSection,PayPalDepositSection,TableBookingForm,ManagementTableBookingForm}.tsx` |
| Logic / constants | `lib/constants.ts`, `lib/sunday-lunch-cutoff.ts`, `lib/api/bookings.ts`, `lib/api/client.ts` (`getSundayLunchMenu`, `createTableBooking`) |
| Tests | `components/features/TableBooking/__tests__/PayPalDepositSection.test.tsx` |

See also: [[overview]], [[routes]], [[server-actions]], [[env-vars]], [[data-model]]
