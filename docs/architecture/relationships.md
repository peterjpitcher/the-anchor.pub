---
generated: true
last_updated: 2026-04-29T00:00:00Z
source: session-setup
project: the-anchor-pub
---

# Relationships

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

A cross-reference map: who calls whom, which file uses which integration, and which UI surfaces are powered by which [[routes#api-routes|API proxy route]].

## Integration -> files

### Management API (`anchorAPI` / direct fetch)

- `lib/api/client.ts` — central typed client; instantiates `anchorAPI = new AnchorAPI(process.env.ANCHOR_API_KEY)`
- `lib/api/private-bookings.ts` — typed wrappers for private-booking endpoints
- `lib/management-api-base.ts` — single source for the upstream base URL
- All `app/api/**/route.ts` — proxy and shape calls before responding to the browser

### PayPal

- `app/api/table-bookings/paypal/create-order/route.ts`
- `app/api/table-bookings/paypal/capture-order/route.ts`
- `app/api/parking/payment/create-order/route.ts`
- `app/api/parking/payment/capture/route.ts`
- Client SDK initialised on `book-table` and `heathrow-parking/[terminal]` pages via `NEXT_PUBLIC_PAYPAL_CLIENT_ID`

### Cloudflare Turnstile

- `lib/turnstile.ts` — server-side token verification
- `lib/spam-protection.ts` — orchestrates Turnstile + heuristic checks for booking forms
- Consumers: `app/api/table-bookings/route.ts`, `app/api/booking/agent/route.ts`, `app/api/public/private-booking/route.ts`, `app/api/event-bookings/route.ts`, `app/api/event-waitlist/route.ts`, `app/api/parking/bookings/route.ts`, `app/api/enquiry/christmas/route.ts`
- Widget rendered by: `components/PrivateBookingInquiryForm.tsx`, `components/features/EventBooking/ManagementEventBookingForm.tsx`, `components/features/TableBooking/ManagementTableBookingForm.tsx`

### Microsoft Graph (enquiry emails)

- `app/api/enquiry/christmas/route.ts`
- This also forwards the structured enquiry to the management API; Graph is the email transport.

### Google Tag Manager + Microsoft Clarity

- `components/tracking/*`, `lib/gtm-events.ts`
- Loaded by `app/layout.tsx`

### AviationStack

- Used by parking pages (`app/heathrow-parking/*`) and any flight-data widgets through helpers in `lib/`

## API route -> UI consumers

| Route | Consumers |
|---|---|
| [[routes#api-table-bookings\|`/api/table-bookings`]] | `app/book-table/page.tsx` (booking wizard), `components/features/TableBooking/ManagementTableBookingForm.tsx` |
| [[routes#api-table-bookings-availability\|`/api/table-bookings/availability`]] | `app/book-table/page.tsx`, `app/api/booking/agent/route.ts` (server-to-server), AI agents over HTTPS |
| `/api/booking/agent` (GET, POST) | External AI agents (GPT-5 etc.) — public structured contract |
| `/api/booking/payment-return` | PayPal redirect target after off-site checkout |
| `/api/business/hours` | `components/StatusBar.tsx` and any "are we open?" widget; powered by `lib/hours-utils.ts` |
| `/api/events*` | `app/whats-on/page.tsx`, `app/events/[id]/page.tsx`, `app/book-event/page.tsx`, `components/features/EventBooking/ManagementEventBookingForm.tsx` |
| `/api/event-categories`, `/api/event-waitlist` | Whats-on filtering, event waitlist forms |
| `/api/calendar/*` | Calendar widgets, ICS endpoints used by event pages |
| `/api/managers-special*` | `app/drinks/managers-special/page.tsx`, drinks landing |
| `/api/parking/*` | `app/heathrow-parking/*`, `app/parking/bookings/[id]/page.tsx` |
| `/api/private-booking-enquiry`, `/api/public/private-booking*` | `components/PrivateBookingInquiryForm.tsx`, `app/private-hire/*` |
| `/api/enquiry/christmas` | `app/christmas-parties/page.tsx`, `app/corporate-christmas-parties/page.tsx` |
| `/api/customers/lookup` | Booking forms (returning-customer prefill) |
| `/api/reviews*` | `app/reviews/page.tsx`, homepage testimonial sections |
| `/api/analytics`, `/api/web-vitals` | `components/tracking/*` (web-vitals beacons) |
| `/api/health` | Uptime probe / monitoring |

## Shared service-window logic

- `lib/table-booking-service-windows.ts` is consumed by:
  - `app/api/table-bookings/route.ts`
  - `app/api/table-bookings/availability/route.ts`
  - `app/api/booking/agent/route.ts`
- `lib/hours-utils.ts` is consumed by:
  - `app/api/business/hours/route.ts`
  - `lib/table-booking-service-windows.ts`
  - `components/StatusBar.tsx` and other "are we open?" surfaces

## See also

- [[overview]] — top-level architecture
- [[routes]] — full route table
- [[server-actions]] — why there are zero of them
- [[data-model]] — upstream endpoints consumed
- [[env-vars]] — env keys feeding the integrations above
