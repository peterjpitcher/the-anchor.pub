---
generated: true
last_updated: 2026-04-29T00:00:00Z
source: session-setup
project: the-anchor-pub
---

# Routes

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

All public URLs handled by the Next.js App Router in this repo. The site has 121 pages, 35 API route files, and 2 layouts. See [[overview]] for the high-level picture and [[server-actions]] for why mutations live under [[routes#api-routes|/api/]] rather than as `'use server'` actions.

## Recently Changed (April 2026)

These three files have shipped material changes in the last few commits — review them first when investigating booking-related issues:

| File | Most recent change |
|---|---|
| `app/api/table-bookings/availability/route.ts` | New `/availability` endpoint that returns combined drinks+food slots with `kitchen_open` per slot. Calls `resolveCombinedServiceRanges` and `buildSlotsWithKitchenState`. Backwards-compatible no-ops for `booking_type` and `purpose` query params. |
| `app/api/table-bookings/route.ts` | Public proxy for booking creation. Strips inbound `sunday_lunch` / `booking_type`, always forwards `booking_type='regular'`. Validates time against `resolveServiceRanges()`. Customer-facing error copy is intentionally neutral (no kitchen/bar references). |
| `app/api/booking/agent/route.ts` | AI-agent booking endpoint (used by GPT-5 + similar). GET delegates to `/api/table-bookings/availability`; POST goes through the same service-window check before calling `anchorAPI.createTableBooking`. |

Supporting libs: `lib/table-booking-service-windows.ts`, `lib/hours-utils.ts`, `lib/api/client.ts`.

## API Routes

All routes below are server-only Next.js Route Handlers under `app/api/**/route.ts`. They authenticate to the management API with `ANCHOR_API_KEY` (server-only). User-submitted forms are gated by Cloudflare Turnstile via `lib/spam-protection.ts`.

### Table Bookings

| URL Path | Method | Auth | Source File |
|---|---|---|---|
| `/api/table-bookings` | POST | `ANCHOR_API_KEY` + Turnstile (verified upstream) | `app/api/table-bookings/route.ts` |
| `/api/table-bookings/availability` | GET | server-only key (via `anchorAPI.getBusinessHours`) | `app/api/table-bookings/availability/route.ts` |
| `/api/table-bookings/create` | (proxy) | `ANCHOR_API_KEY` | `app/api/table-bookings/create/route.ts` |
| `/api/table-bookings/[reference]` | GET, DELETE | `ANCHOR_API_KEY` | `app/api/table-bookings/[reference]/route.ts` |
| `/api/table-bookings/paypal/create-order` | POST | `ANCHOR_API_KEY` (Bearer) | `app/api/table-bookings/paypal/create-order/route.ts` |
| `/api/table-bookings/paypal/capture-order` | POST | `ANCHOR_API_KEY` (Bearer) | `app/api/table-bookings/paypal/capture-order/route.ts` |
| `/api/booking/agent` | GET, POST | `ANCHOR_API_KEY` + Turnstile | `app/api/booking/agent/route.ts` |
| `/api/booking/payment-return` | GET | none (PayPal redirect target) | `app/api/booking/payment-return/route.ts` |
| `/api/bookings/initiate` | POST | `ANCHOR_API_KEY` | `app/api/bookings/initiate/route.ts` |

### Events

| URL Path | Method | Auth | Source File |
|---|---|---|---|
| `/api/events` | GET | `ANCHOR_API_KEY` | `app/api/events/route.ts` |
| `/api/events/[id]` | GET | `ANCHOR_API_KEY` | `app/api/events/[id]/route.ts` |
| `/api/events/[id]/availability` | POST | `ANCHOR_API_KEY` | `app/api/events/[id]/availability/route.ts` |
| `/api/event-bookings` | POST | `ANCHOR_API_KEY` + Turnstile | `app/api/event-bookings/route.ts` |
| `/api/event-categories` | GET | `ANCHOR_API_KEY` | `app/api/event-categories/route.ts` |
| `/api/event-waitlist` | POST | `ANCHOR_API_KEY` + Turnstile | `app/api/event-waitlist/route.ts` |
| `/api/calendar/upcoming` | GET | `ANCHOR_API_KEY` | `app/api/calendar/upcoming/route.ts` |
| `/api/calendar/event/[id]` | GET | `ANCHOR_API_KEY` | `app/api/calendar/event/[id]/route.ts` |

### Hours, Menu & Content

| URL Path | Method | Auth | Source File |
|---|---|---|---|
| `/api/business/hours` | GET | `ANCHOR_API_KEY` (no-store cached) | `app/api/business/hours/route.ts` |
| `/api/managers-special` | GET | `ANCHOR_API_KEY` | `app/api/managers-special/route.ts` |
| `/api/managers-special-image` | GET | `ANCHOR_API_KEY` | `app/api/managers-special-image/route.ts` |
| `/content/blog/[...path]` | GET | `MS_PREVIEW_TOKEN` (preview only) | `app/content/blog/[...path]/route.ts` |

### Enquiries & Private Bookings

| URL Path | Method | Auth | Source File |
|---|---|---|---|
| `/api/enquiry/christmas` | POST | `ANCHOR_API_KEY` + Microsoft Graph + Turnstile | `app/api/enquiry/christmas/route.ts` |
| `/api/enquiry/open-mic` | POST | `ANCHOR_API_KEY` + Microsoft Graph + Turnstile | `app/api/enquiry/open-mic/route.ts` |
| `/api/private-booking-enquiry` | POST | `ANCHOR_API_KEY` + Turnstile | `app/api/private-booking-enquiry/route.ts` |
| `/api/public/private-booking` | POST | `ANCHOR_API_KEY` + Turnstile | `app/api/public/private-booking/route.ts` |
| `/api/public/private-booking/config` | GET | `ANCHOR_API_KEY` | `app/api/public/private-booking/config/route.ts` |
| `/api/customers/lookup` | GET | `ANCHOR_API_KEY` | `app/api/customers/lookup/route.ts` |

### Parking (Heathrow)

| URL Path | Method | Auth | Source File |
|---|---|---|---|
| `/api/parking/availability` | GET | `ANCHOR_API_KEY` | `app/api/parking/availability/route.ts` |
| `/api/parking/rates` | GET | `ANCHOR_API_KEY` | `app/api/parking/rates/route.ts` |
| `/api/parking/bookings` | POST | `ANCHOR_API_KEY` + Turnstile | `app/api/parking/bookings/route.ts` |
| `/api/parking/bookings/[id]` | GET | `ANCHOR_API_KEY` | `app/api/parking/bookings/[id]/route.ts` |
| `/api/parking/payment/create-order` | POST | `ANCHOR_API_KEY` | `app/api/parking/payment/create-order/route.ts` |
| `/api/parking/payment/capture` | POST | `ANCHOR_API_KEY` | `app/api/parking/payment/capture/route.ts` |

### Reviews & Ops

| URL Path | Method | Auth | Source File |
|---|---|---|---|
| `/api/reviews` | GET | `ANCHOR_API_KEY` | `app/api/reviews/route.ts` |
| `/api/reviews/status` | GET | `ANCHOR_API_KEY` | `app/api/reviews/status/route.ts` |
| `/api/analytics` | GET, POST | none (public beacon) | `app/api/analytics/route.ts` |
| `/api/web-vitals` | POST | none (public beacon) | `app/api/web-vitals/route.ts` |
| `/api/health` | GET | none | `app/api/health/route.ts` |

## Pages

The site has **121 page files**. They fall into these groups:

### Marketing — Core

`/` (home), `/about`, `/our-pub`, `/find-us`, `/sustainability`, `/safety-and-respect`, `/accessibility`, `/privacy-policy`, `/sitemap-page`, `/reviews`, `/leave-review`.

### Food, Drink & Menus

`/sunday-lunch`, `/food-menu`, `/food-menu/gluten-free`, `/food-menu/vegan`, `/food-menu/vegetarian`, `/burger-menu`, `/pizza-menu`, `/fish-and-chips-heathrow`, `/drinks`, `/drinks/[slug]`, `/drinks/baby-guinness`, `/drinks/managers-special`.

### Booking & Enquiry Flows

`/book-table`, `/book-event`, `/booking-confirmation`, `/private-hire` (+ layout), `/private-hire/baby-showers`, `/private-hire/christenings`, `/private-hire/engagement-parties`, `/private-hire/gender-reveal`, `/private-hire/milestone-birthdays`, `/private-hire/retirement-parties`, `/private-hire/wakes`, `/private-hire/near/[slug]`, `/private-party-venue`, `/function-room-hire`, `/corporate-events`, `/christmas-parties`, `/corporate-christmas-parties`.

### Heathrow Parking

`/heathrow-parking`, `/heathrow-parking/[terminal]`, `/heathrow-parking/confirmation/[bookingId]`, `/parking/bookings/[id]`, `/free-parking`, `/coach-parking-heathrow`.

### Events / What's On

`/whats-on`, `/whats-on/drag-shows`, `/events/[id]`, `/cash-bingo`, `/music-bingo`, `/quiz-night`, `/karaoke`, `/open-mic`, `/live-music`, `/live-sport`, `/live-sport/boxing`, `/live-sport/f1`, `/live-sport/six-nations`, `/live-sport/world-cup`, `/easter`, `/halloween`, `/bonfire-night`, `/new-years-eve`, `/boxing-day`, `/valentines-day`, `/mothers-day`, `/fathers-day`, `/st-patricks-day`, `/bank-holiday-weekends`, `/summer-garden-parties`.

### Local SEO — Areas & Hotels

`/ashford-pub`, `/bedfont-pub`, `/colnbrook-pub`, `/egham-pub`, `/feltham-pub`, `/horton-pub`, `/longford-pub`, `/staines-pub`, `/stanwell-pub`, `/sunbury-pub`, `/windsor-pub`, `/wraysbury-pub`, `/pubs-in-stanwell`, `/m25-junction-14-pub`, `/near-heathrow`, `/near-heathrow/terminal-2`, `/near-heathrow/terminal-3`, `/near-heathrow/terminal-4`, `/near-heathrow/terminal-5`, `/restaurants-near-heathrow`, `/heathrow-family-dining`, `/heathrow-hotels-pub`, `/heathrow-layover-dining`, `/luggage-storage-heathrow`, `/plane-spotting-heathrow`, `/pre-flight-meal`, `/dog-friendly-pub-heathrow`, `/family-friendly-pub-heathrow`, `/pool-darts-pub`, `/pub-garden-heathrow`, `/beer-garden`, plus the `/pub-near-*` hotel set (Crowne Plaza, Hilton, Holiday Inn, Ibis, Marriott, Novotel, Premier Inn, Radisson Blu, Renaissance, Sofitel, Travelodge).

### Blog

`/blog`, `/blog/[slug]`, `/blog/tags`, `/blog/tag/[tag]`.

### Catch-all

`/[...unmatched]` — custom 404.

## Layouts

| Path | Source File |
|---|---|
| Root | `app/layout.tsx` |
| Private hire | `app/private-hire/layout.tsx` |

## Cron

None. Scheduled work happens in the management app, not here.

## Middleware

`middleware.ts` matches everything except `_next/static`, `_next/image`, `favicon.ico`. Responsibilities: HTTPS + www canonicalisation, trailing-slash normalisation, blog `?page=1` redirect, security headers, cache-control for `/api/*` (no-store on `/api/business/hours`, `s-maxage=60, stale-while-revalidate=300` elsewhere) and static assets.

See [[overview#auth-model]] and [[env-vars]].
