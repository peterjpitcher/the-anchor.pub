---
generated: true
last_updated: 2026-05-11T00:00:00Z
source: session-setup
project: the-anchor-pub
---

# Server Actions

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

## Status

**No server actions found.** This project does not use the `'use server'` directive.

All mutations are handled via Next.js API route handlers (`app/api/*/route.ts`) which proxy requests to the management API at `management.orangejelly.co.uk`. This is the expected pattern for this project since it has no database of its own.

## Mutation Flow

```
Client Component (form submit / button click)
    |
    v
fetch('/api/...', { method: 'POST', body: ... })
    |
    v
API Route Handler (app/api/*/route.ts)
    |  - Validates input (Zod)
    |  - Adds ANCHOR_API_KEY header
    |  - Optionally validates Turnstile token
    v
Management API (management.orangejelly.co.uk)
```

## Key Mutation Endpoints

| Endpoint | Purpose |
|----------|---------|
| `POST /api/table-bookings` | Create table booking |
| `POST /api/event-bookings` | Create event booking |
| `POST /api/event-waitlist` | Join event waitlist |
| `POST /api/parking/bookings` | Create parking booking |
| `POST /api/public/private-booking` | Submit private hire enquiry |
| `POST /api/enquiry/christmas` | Send Christmas party enquiry email |
| `POST /api/table-bookings/paypal/create-order` | Initiate PayPal deposit payment |
| `POST /api/table-bookings/paypal/capture-order` | Capture PayPal deposit payment |
| `POST /api/parking/payment/create-order` | Initiate PayPal parking payment |
| `POST /api/parking/payment/capture` | Capture PayPal parking payment |
| `DELETE /api/table-bookings/[reference]` | Cancel a table booking |

## Related Docs

- [[routes]] -- Full route table with auth details
- [[relationships]] -- Cross-reference map
