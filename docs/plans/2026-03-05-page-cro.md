# Page CRO Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Apply conversion rate optimisation changes across all site pages, prioritised by GSC traffic volume, to increase table bookings and phone enquiries.

**Architecture:** Targeted JSX/copy edits to existing page files. No new pages. No new components — `ReviewsBadge` (already at `components/reviews/ReviewsBadge.tsx`) replaces the planned "SocialProofStrip". Each task is one page or a closely related group. All changes verified with build + typecheck before committing.

**Tech Stack:** Next.js 14 App Router, TypeScript, Tailwind CSS. Relevant existing components: `BookTableButton`, `PhoneButton`, `CTASection`, `HeroWrapper`, `ReviewsBadge`, `ReviewSection`, `GoogleReviews`, `FoodStickyCtaBar`.

**Rating data (confirmed from schema):** 4.8 stars, 127 reviews — use these values wherever `ReviewsBadge` is added.

**Design doc:** `docs/plans/2026-03-05-page-cro-design.md`

---

## Task 1: Homepage — hero value prop + trust badge

**Files:**
- Modify: `app/page.tsx` (lines ~116–168)

**Context:**
The hero currently shows a seasonal greeting as the headline (e.g. "Spring at The Anchor"). A first-time visitor cannot understand what this place is within 5 seconds. Below the logo there is a tagline "Where Everyone's Welcome" with no specific benefit. There is a trust badge but it doesn't show an actual rating number.

**Step 1: Read the current hero section**

Open `app/page.tsx` and locate:
- The `<p>` tag with "Where Everyone's Welcome" (~line 147)
- The trust badge `<span>` with "Highest-rated non-airport pub in the Heathrow area" (~line 158)

**Step 2: Add a value prop sub-headline below "Where Everyone's Welcome"**

After the existing `<p className="text-2xl sm:text-3xl text-white font-serif ...">Where Everyone's Welcome</p>` line, add:

```tsx
<p className="text-base sm:text-lg text-white/90 font-medium drop-shadow-[0_1px_3px_rgba(0,0,0,0.8)] max-w-xl mx-auto text-center px-4">
  The closest traditional British pub to Heathrow — 7 mins from T5, free parking, proper food
</p>
```

**Step 3: Update the trust badge to include actual rating**

Change the existing trust badge span from:
```tsx
⭐ Highest-rated non-airport pub in the Heathrow area
```
to:
```tsx
⭐ 4.8/5 · 127 Google Reviews · Highest-rated non-airport pub near Heathrow
```

**Step 4: Verify**

```bash
cd /Users/peterpitcher/Cursor/the-anchor.pub
npx tsc --noEmit
npm run lint
```

Expected: no errors.

**Step 5: Commit**

```bash
git add app/page.tsx
git commit -m "feat(cro): add value prop sub-headline and rating to homepage hero"
```

---

## Task 2: Homepage — rewrite feature cards + add mid-page CTA

**Files:**
- Modify: `app/page.tsx` (lines ~276–310, ~311+)

**Context:**
The "What Makes Us Special" FeatureGrid has 3 cards with company-centric titles: "Community Hub", "Honest Food", "Events & Entertainment". These describe the pub, not the visitor's benefit. There is also no CTA after this section — a long content gap before the next conversion point.

**Step 1: Read the FeatureGrid section**

Locate the `<FeatureGrid>` component around line 274 in `app/page.tsx`. It has 3 feature objects with `title` and `description` fields.

**Step 2: Rewrite the 3 feature card titles and descriptions**

Replace the 3 feature objects inside `FeatureGrid` with:

```tsx
{
  icon: "💰",
  title: "Eat Well, Spend Less",
  description: "Airport food costs twice as much. Enjoy a proper British pub meal from £10 — 7 minutes from the terminals, no terminal markup.",
  variant: "colored",
  color: "bg-anchor-sand/30",
  className: "card-warm p-8 text-center"
},
{
  icon: "✈️",
  title: "Perfect for Heathrow Trips",
  description: "Pre-flight meal, meeting arrivals, or killing layover time. Free parking, luggage welcome, and just 7 minutes from Terminal 5.",
  variant: "colored",
  color: "bg-anchor-sand/30",
  className: "card-warm p-8 text-center"
},
{
  icon: "🐕",
  title: "Bring the Whole Family",
  description: "Dog-friendly beer garden under the Heathrow flight path. Watch planes every 90 seconds. Kids and dogs both welcome.",
  variant: "colored",
  color: "bg-anchor-sand/30",
  className: "card-warm p-8 text-center"
}
```

**Step 3: Add a BookTableButton after the FeatureGrid closing tag**

After the `</FeatureGrid>` and its wrapping `</Container>` / `</div>`, add:

```tsx
<div className="mt-10 text-center">
  <BookTableButton
    source="homepage_features_cta"
    variant="primary"
    size="lg"
  />
</div>
```

Place this inside the existing white section's `<Container>` block, after the FeatureGrid.

**Step 4: Update the bottom "Come Visit Us!" heading**

Find the `<h2>` around line 688 that reads "Come Visit Us!" and change it to:

```tsx
<h2 className="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4">
  Ready for a proper pub near Heathrow?
</h2>
```

**Step 5: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 6: Commit**

```bash
git add app/page.tsx
git commit -m "feat(cro): rewrite homepage feature cards and add mid-page CTA"
```

---

## Task 3: Beer Garden — reduce hero CTAs + add price signal + remove smoking feature

**Files:**
- Modify: `app/beer-garden/page.tsx` (lines ~89–143, ~304–355)

**Context:**
The hero has 1 primary CTA + 3 secondary CTAs. "Get Directions" in the hero is low-intent and dilutes the primary "Book a Table" action. No price signals exist on the page. "Smoking Area" is listed in the feature grid alongside "Dog Friendly" and "Family Friendly" — poor optics for families.

**Step 1: Remove "Get Directions" from the secondary CTAs**

In the `secondaryCta` prop of `<HeroWrapper>` (around line 109), the current JSX renders 3 `<Link>` buttons:
- ✈️ Plane Spotting Guide
- 🍺 View Drinks Menu
- 📍 Get Directions

Remove the entire third `<Link>` block for Get Directions:
```tsx
// DELETE THIS:
<Link href="/find-us">
  <Button
    variant="secondary"
    size="lg"
    className="w-full sm:w-auto"
  >
    📍 Get Directions
  </Button>
</Link>
```

**Step 2: Add price anchor to the hero description**

The hero `description` prop is currently:
```tsx
description="Watch aircraft every 90 seconds while enjoying a pint"
```

Change to:
```tsx
description="Watch aircraft every 90 seconds while enjoying a pint — food from £10, dogs welcome"
```

**Step 3: Add ReviewsBadge after the food cards section**

After the closing `</Section>` of the "Grab Food & Deals" section (around line 213), import and add `ReviewsBadge`:

First, add the import at the top of the file (with the other review imports or after existing imports):
```tsx
import { ReviewsBadge } from '@/components/reviews/ReviewsBadge'
```

Then add a new section:
```tsx
<section className="bg-white py-6">
  <Container>
    <div className="flex justify-center">
      <ReviewsBadge rating={4.8} totalReviews={127} size="medium" />
    </div>
  </Container>
</section>
```

**Step 4: Remove "Smoking Area" from the FeatureGrid**

In the `<FeatureGrid>` around line 312, find the feature object with `title: "Smoking Area"` and delete the entire object (including trailing comma). The grid will reflow from 6 to 5 items automatically.

**Step 5: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 6: Commit**

```bash
git add app/beer-garden/page.tsx
git commit -m "feat(cro): reduce hero CTAs, add price anchor and review badge to beer garden"
```

---

## Task 4: Near Heathrow — fix hero CTA + add reviews + fix bottom CTA + add sticky bar

**Files:**
- Modify: `app/near-heathrow/page.tsx` (lines ~39–78, ~804–824)

**Context:**
The hero primary CTA label says "📞 Book a Table: 01753 682707" inside a `BookTableButton` — this conflates two different actions. The hero secondary CTA "Get Directions" scrolls to #terminals, which is low-conversion intent. The bottom CTASection has no Book Table option. No reviews section exists anywhere on this long (840-line) page.

**Step 1: Fix the hero primary CTA label**

Find the `<BookTableButton>` inside `primaryCta` (around line 55). It currently renders:
```tsx
>
  📞 Book a Table: 01753 682707
</BookTableButton>
```

Remove the custom children so it uses the default "Book a Table" label:
```tsx
<BookTableButton
  source="near_heathrow_hero"
  context="heathrow_traveler"
  variant="primary"
  size="lg"
  className="w-full sm:w-auto"
/>
```

**Step 2: Add a PhoneButton as a second secondary CTA**

The current `secondaryCta` is a single "Get Directions" button. Change it to render two buttons:

First, confirm `PhoneButton` is imported. Add to imports if missing:
```tsx
import { PhoneButton } from '@/components/PhoneButton'
```

Then replace `secondaryCta`:
```tsx
secondaryCta={
  <>
    <Link href="#terminals" className="w-full sm:w-auto">
      <Button
        variant="secondary"
        size="lg"
        fullWidth
        className="sm:w-auto"
      >
        📍 View Terminal Directions
      </Button>
    </Link>
    <PhoneButton
      phone="01753 682707"
      source="near_heathrow_hero"
      variant="secondary"
      size="lg"
      className="w-full sm:w-auto"
    >
      📞 Call Us
    </PhoneButton>
  </>
}
```

**Step 3: Add ReviewsBadge import**

```tsx
import { ReviewsBadge } from '@/components/reviews/ReviewsBadge'
```

**Step 4: Add a reviews section after "Why Travelers Love The Anchor"**

Find the closing `</section>` of the "Why Travelers Love The Anchor" section (around line 241). After it, add:

```tsx
<section className="py-10 bg-anchor-cream/30">
  <Container>
    <div className="flex flex-col items-center gap-6">
      <ReviewsBadge rating={4.8} totalReviews={127} size="medium" />
      <p className="text-center text-gray-600 text-sm max-w-md">
        Rated the highest non-airport pub near Heathrow by our guests
      </p>
    </div>
  </Container>
</section>
```

**Step 5: Add BookTableButton to the bottom CTASection**

Find the `<CTASection>` near the bottom of the file (around line 806). It currently has two buttons (Phone + Directions). Add a Book Table button:

```tsx
<CTASection
  title="Visit The Anchor - Heathrow's Local"
  description="Just minutes from all terminals. Free parking. Great food. Genuine British pub experience."
  buttons={[
    {
      text: "Book a Table",
      href: "/book-table",
      variant: "white"
    },
    {
      text: "📞 Call: 01753 682707",
      href: "tel:+441753682707",
      variant: "white"
    },
    {
      text: "📍 Get Directions",
      href: "https://maps.google.com/maps?q=The+Anchor+Stanwell+Moor+TW19+6AQ",
      variant: "white",
      external: true
    }
  ]}
  variant="green"
  footer="The Anchor, Horton Road, Stanwell Moor, Surrey TW19 6AQ\nFree Parking • Family Friendly • Dog Friendly • Garden • Late Opening"
/>
```

**Step 6: Add FoodStickyCtaBar**

Add the import:
```tsx
import { FoodStickyCtaBar } from '@/components/food/FoodStickyCtaBar'
```

Add the component at the top of the returned JSX, before `<SpeakableSchema />`:
```tsx
<FoodStickyCtaBar />
```

**Step 7: Verify**

```bash
npx tsc --noEmit
npm run lint
```

Check that FoodStickyCtaBar works as a Server Component (it does on food-menu and sunday-lunch). If it requires `'use client'`, add that directive to the near-heathrow page or wrap in a dynamic import with `ssr: true`.

**Step 8: Commit**

```bash
git add app/near-heathrow/page.tsx
git commit -m "feat(cro): fix hero CTA, add reviews badge and book table to near-heathrow page"
```

---

## Task 5: Food Menu — add price signal to hero

**Files:**
- Modify: `app/food-menu/page.tsx` (lines ~239–281)

**Context:**
The hero already has `BookTableButton` as primary CTA and a good description. It needs a price anchor to counter the "expensive near Heathrow" objection. The `MenuAnchorNav` includes a "Near Heathrow" nav item which may distract from booking — evaluate and remove if it doesn't serve a clear purpose.

**Step 1: Add price signal to hero description**

Current description (line ~242):
```tsx
description="Sunday roasts, stone-baked pizzas and proper pub classics with free parking and rapid service."
```

Change to:
```tsx
description="Sunday roasts from £19.99, stone-baked pizzas and proper pub classics. Free parking, 7 mins from Heathrow — no airport prices."
```

**Step 2: Evaluate the "Near Heathrow" anchor nav item**

Find `ANCHOR_LINKS` at the top of the file (~line 27). It contains:
```tsx
{ id: 'near-heathrow', label: 'Near Heathrow', icon: '✈️' }
```

Check whether there is a matching `id="near-heathrow"` section in the page body. If this anchor nav item doesn't scroll to a useful section within the menu, remove it from `ANCHOR_LINKS`:
```tsx
// Remove this item:
{ id: 'near-heathrow', label: 'Near Heathrow', icon: '✈️' }
```

**Step 3: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 4: Commit**

```bash
git add app/food-menu/page.tsx
git commit -m "feat(cro): add price anchor to food menu hero description"
```

---

## Task 6: Sunday Lunch — reframe deposit copy

**Files:**
- Modify: `app/sunday-lunch/page.tsx` (lines ~400–406)

**Context:**
The hero already has urgency ("Book by Saturday 1pm"), `BookTableButton`, and the `FoodStickyCtaBar`. The only CRO change needed is reframing the deposit tag from negative obligation to positive commitment signal.

**Step 1: Read the hero tags**

At lines ~402–406, the tags array is:
```tsx
tags={[
  { label: sundayServiceLabel, variant: 'warning' },
  { label: 'Book by Saturday 1pm', variant: 'default' },
  { label: '£10pp deposit required', variant: 'default' }
]}
```

**Step 2: Reframe the deposit tag**

Change:
```tsx
{ label: '£10pp deposit required', variant: 'default' }
```
to:
```tsx
{ label: '£10pp deposit — secures your table', variant: 'success' }
```

The `variant: 'success'` (green) signals reassurance rather than obligation.

**Step 3: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 4: Commit**

```bash
git add app/sunday-lunch/page.tsx
git commit -m "feat(cro): reframe sunday lunch deposit tag as positive commitment signal"
```

---

## Task 7: Private Hire hub — rewrite hero + PageTitle + add social proof

**Files:**
- Modify: `app/private-hire/page.tsx` (lines ~30–80, ~63–66)

**Context:**
The hero description is generic ("The perfect venue for your special occasion near Heathrow"). PageTitle says "Hosting Life's Important Moments" — vague. Primary CTA is a plain `<Button>` inside `<Link>` — not tracked. No social proof visible.

**Step 1: Rewrite the hero description**

Current:
```tsx
description="The perfect venue for your special occasion near Heathrow"
```

Change to:
```tsx
description="Private rooms for 10–200 guests · Free parking for all · Buffet packages available · 7 mins from Heathrow"
```

**Step 2: Rewrite the PageTitle**

Find the `<PageTitle>` component (around line 65):
```tsx
<PageTitle className="text-center mb-8" seo={{ structured: true, speakable: true }}>
  Hosting Life's Important Moments
</PageTitle>
```

Change to:
```tsx
<PageTitle className="text-center mb-8" seo={{ structured: true, speakable: true }}>
  Your Event, Your Space — Private Hire at The Anchor
</PageTitle>
```

**Step 3: Replace the untracked primary CTA with PhoneButton**

Current primary CTA (~line 44):
```tsx
primaryCta={
  <Link href="/private-hire#enquiry" className="w-full sm:w-auto">
    <Button variant="primary" size="lg" className="w-full sm:w-auto">
      Enquire Now
    </Button>
  </Link>
}
```

Replace with `PhoneButton` (already imported):
```tsx
primaryCta={
  <PhoneButton
    phone="01753 682707"
    source="private_hire_hero_primary"
    variant="primary"
    size="lg"
    className="w-full sm:w-auto"
  >
    📞 Call to Discuss Your Event
  </PhoneButton>
}
```

Keep the existing `PhoneButton` secondary CTA as-is but change its label:
```tsx
secondaryCta={
  <Link href="/private-hire#enquiry" className="w-full sm:w-auto">
    <Button variant="secondary" size="lg" className="w-full sm:w-auto">
      Enquire Online
    </Button>
  </Link>
}
```

**Step 4: Add ReviewsBadge above the event-type grid**

Import `ReviewsBadge` if not already imported:
```tsx
import { ReviewsBadge } from '@/components/reviews/ReviewsBadge'
```

After the `<PageTitle>` and before the event-type grid (`<div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-16">`), add:

```tsx
<div className="flex justify-center mb-10">
  <ReviewsBadge rating={4.8} totalReviews={127} size="small" />
</div>
```

**Step 5: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 6: Commit**

```bash
git add app/private-hire/page.tsx
git commit -m "feat(cro): rewrite private hire hero, add phone CTA and review badge"
```

---

## Task 8: Wakes — replace BookTableButton with PhoneButton as primary CTA

**Files:**
- Modify: `app/private-hire/wakes/page.tsx` (lines ~48–60)

**Context:**
The wakes page uses `BookTableButton` as the primary CTA ("Check Availability"). A funeral reception enquiry should go via phone, not a table booking flow. Using a dining booking tool on a wakes page is tonally wrong and creates unnecessary friction.

**Step 1: Read the hero CTA section (lines 48–70)**

Confirm the current structure:
```tsx
primaryCta={
  <BookTableButton
    source="wakes_hero"
    variant="primary"
    size="lg"
    context="wakes"
  >
    Check Availability
  </BookTableButton>
}
secondaryCta={
  <PhoneButton
    phone="01753 682707"
    ...
  >
    Call 01753 682707
  </PhoneButton>
}
```

**Step 2: Swap primary and secondary CTAs**

Replace with:
```tsx
primaryCta={
  <PhoneButton
    phone="01753 682707"
    source="wakes_hero_primary"
    variant="primary"
    size="lg"
    className="w-full sm:w-auto"
  >
    📞 Call to Discuss Arrangements
  </PhoneButton>
}
secondaryCta={
  <Link href="/private-hire#enquiry" className="w-full sm:w-auto">
    <Button variant="secondary" size="lg" className="w-full sm:w-auto">
      Enquire Online
    </Button>
  </Link>
}
```

Add `Link` and `Button` imports if not already present:
```tsx
import Link from 'next/link'
import { Button } from '@/components/ui'
```

**Step 3: Check the bottom of the page**

Scroll to the bottom of the file and confirm there is a phone CTA (not a BookTableButton). If `PrivateBookingSection` is used at the bottom, check whether it renders a phone option — if it only shows a booking button, add a `PhoneButton` above it.

**Step 4: Remove unused BookTableButton import if no longer needed**

If `BookTableButton` is no longer used anywhere in the file, remove its import line.

**Step 5: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 6: Commit**

```bash
git add app/private-hire/wakes/page.tsx
git commit -m "feat(cro): replace book table CTA with phone CTA on wakes page"
```

---

## Task 9: Pizza Tuesday — remove voucher friction + add urgency + pizza image

**Files:**
- Modify: `app/pizza-tuesday/page.tsx` (lines ~60+)

**Context:**
545 impressions, 0 clicks. The page uses a generic fallback image and a FAQ about vouchers suggests a common friction point. Visitors need to know upfront: no voucher required, just book.

**Step 1: Read the hero section of pizza-tuesday/page.tsx**

Read from line 60 onwards to find the `<HeroWrapper>` and its `primaryCta` and `description` props.

**Step 2: Add "no voucher needed" line to hero description**

If the current description is something like "Get 2-for-1 stone-baked pizzas every Tuesday at The Anchor...", append:

```tsx
description="Buy one stone-baked pizza, get one free every Tuesday. No voucher needed — just book a table and mention Pizza Tuesday."
```

**Step 3: Confirm or add BookTableButton as primary CTA**

If the hero already has a `BookTableButton`, confirm it is present. If missing, add:

```tsx
primaryCta={
  <BookTableButton
    source="pizza_tuesday_hero"
    context="pizza"
    variant="primary"
    size="lg"
    className="w-full sm:w-auto"
  >
    Book a Table
  </BookTableButton>
}
```

**Step 4: Add urgency tag**

In the hero `tags` array, add or update to include:
```tsx
{ label: '🍕 Every Tuesday', variant: 'success' },
{ label: '2-for-1 Stone-Baked Pizzas', variant: 'default' },
{ label: 'No Voucher Needed', variant: 'default' }
```

**Step 5: Check for a pizza image**

Run:
```bash
ls /Users/peterpitcher/Cursor/the-anchor.pub/public/images/food/ | grep -i pizza
```

If a pizza image exists (e.g. `pizza.jpg` or similar), update the hero image or the `DEFAULT_PAGE_HEADER_IMAGE` usage. The `HeroWrapper` uses the `route` prop to look up an image from a config — check `lib/hero-images.ts` or equivalent to add a pizza-tuesday entry if not present.

If no pizza image exists, note this as a future content task and skip the image change.

**Step 6: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 7: Commit**

```bash
git add app/pizza-tuesday/page.tsx
git commit -m "feat(cro): add voucher-free messaging and urgency tags to pizza tuesday page"
```

---

## Task 10: Function Room Hire — read page and fix CTA hierarchy

**Files:**
- Modify: `app/function-room-hire/page.tsx` (lines ~76+)

**Context:**
Page has 3 clicks from 325 impressions. The function-room-hire page has `PhoneButton` and `BookTableButton` imported. Need to confirm the hero CTA order and ensure the enquiry/phone CTA is primary.

**Step 1: Read the hero section (lines 76–150)**

Look for the `primaryCta` and `secondaryCta` props on `<HeroWrapper>`.

**Step 2: Ensure PhoneButton is the primary CTA**

For a function room hire enquiry (like private hire), a phone call is more appropriate than a booking button. If `BookTableButton` is currently primary, swap so `PhoneButton` is primary:

```tsx
primaryCta={
  <PhoneButton
    phone="01753 682707"
    source="function_room_hero"
    variant="primary"
    size="lg"
    className="w-full sm:w-auto"
  >
    📞 Call to Check Availability
  </PhoneButton>
}
secondaryCta={
  <Link href="#enquiry" className="w-full sm:w-auto">
    <Button variant="secondary" size="lg" className="w-full sm:w-auto">
      Enquire Online
    </Button>
  </Link>
}
```

**Step 3: Add capacity/pricing to hero description**

Ensure the hero `description` includes capacity and a pricing signal. If it doesn't already, add:

```tsx
description="Flexible rooms for 10–200 guests with free parking, AV support and custom catering. Tailored pricing for every event."
```

**Step 4: Add ReviewsBadge**

Import and add `ReviewsBadge` near the top of the page body, below the hero:

```tsx
import { ReviewsBadge } from '@/components/reviews/ReviewsBadge'
```

Add after the first content section:
```tsx
<section className="bg-white py-6">
  <Container>
    <div className="flex justify-center">
      <ReviewsBadge rating={4.8} totalReviews={127} size="small" />
    </div>
  </Container>
</section>
```

**Step 5: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 6: Commit**

```bash
git add app/function-room-hire/page.tsx
git commit -m "feat(cro): set phone as primary CTA and add review badge to function room hire"
```

---

## Task 11: Pizza Tuesday — check and fix image

**Files:**
- Check: `public/images/food/`
- Possibly modify: `lib/hero-images.ts` or equivalent image config

**Context:**
The pizza-tuesday page uses `DEFAULT_PAGE_HEADER_IMAGE` (generic pub image). A pizza-specific image would improve relevance and on-page credibility.

**Step 1: Check available pizza images**

```bash
ls /Users/peterpitcher/Cursor/the-anchor.pub/public/images/food/
find /Users/peterpitcher/Cursor/the-anchor.pub/public/images -name "*pizza*" -o -name "*Pizza*" 2>/dev/null
```

**Step 2: Find how HeroWrapper resolves images**

```bash
grep -r "pizza-tuesday" /Users/peterpitcher/Cursor/the-anchor.pub/lib/ --include="*.ts" --include="*.tsx" -l
grep -r "pizza" /Users/peterpitcher/Cursor/the-anchor.pub/lib/hero-images* 2>/dev/null | head -10
```

**Step 3: Add pizza-tuesday image entry if possible**

If a pizza image exists and the hero image config supports it, add an entry for `/pizza-tuesday` pointing to the pizza image.

If no pizza image exists, this step is a no-op. Note it in a comment in the file for future content work:
```tsx
// TODO: replace DEFAULT_PAGE_HEADER_IMAGE with a pizza-specific image when available
```

**Step 4: Commit if changed**

```bash
git add -A
git commit -m "feat(cro): use pizza-specific hero image on pizza-tuesday page"
```

---

## Task 12: Tier 3 — Terminal pages (near-heathrow/terminal-2 through terminal-5)

**Files:**
- Modify: `app/near-heathrow/terminal-2/page.tsx` (or equivalent dynamic route)
- Check: `app/near-heathrow/[terminal]/page.tsx`

**Context:**
All four terminal pages share a template (likely a dynamic route `[terminal]`). Apply the standard CRO checklist uniformly.

**Step 1: Find the terminal pages route**

```bash
ls /Users/peterpitcher/Cursor/the-anchor.pub/app/near-heathrow/
```

If there's a `[terminal]` dynamic route, read `app/near-heathrow/[terminal]/page.tsx`. If they are separate files, read `app/near-heathrow/terminal-2/page.tsx`.

**Step 2: Apply standard CRO checklist**

For each terminal page (or the shared template):

- [ ] **Hero primary CTA:** Confirm `BookTableButton` or add it
- [ ] **Hero secondary CTA:** Confirm phone option exists
- [ ] **ReviewsBadge:** Add after hero if not present (import + `<ReviewsBadge rating={4.8} totalReviews={127} size="small" />`)
- [ ] **Mid-page CTA:** Add a `BookTableButton` at a natural mid-page stopping point if none exists
- [ ] **Bottom CTA:** Ensure it includes Book Table + Phone + Directions (not just directions)

**Step 3: Verify**

```bash
npx tsc --noEmit
npm run lint
```

**Step 4: Commit**

```bash
git add app/near-heathrow/
git commit -m "feat(cro): apply standard CRO checklist to Heathrow terminal pages"
```

---

## Task 13: Tier 3 — Event and location pages

**Files:**
- Modify: `app/quiz-night/page.tsx`
- Modify: `app/whats-on/page.tsx`
- Modify: `app/live-sport/page.tsx`
- Modify: `app/karaoke/page.tsx`
- Modify: `app/stanwell-pub/page.tsx` (and similar location pages)

**Context:**
These pages have low click volumes but real impression share. Apply the standard CRO checklist to each. For event pages, the primary CTA should be a `PhoneButton` or `BookTableButton` depending on whether the event requires booking. For location pages, `BookTableButton` is primary.

**Step 1: Read each page's hero section**

For each file, read the first 80 lines to find the `primaryCta` and `secondaryCta` props.

**Step 2: Apply standard CRO checklist per page**

For each page:

- [ ] **Hero primary CTA:** `BookTableButton` for location/food pages; `PhoneButton` for event enquiry pages
- [ ] **Add ReviewsBadge** below hero if not present
- [ ] **Mid-page CTA:** Add at a natural stopping point
- [ ] **Bottom CTA:** Confirm Book Table + Phone (not just directions)

**Step 3: Verify after each file**

```bash
npx tsc --noEmit
npm run lint
```

**Step 4: Commit per page or in logical groups**

```bash
git add app/quiz-night/page.tsx app/whats-on/page.tsx app/live-sport/page.tsx app/karaoke/page.tsx
git commit -m "feat(cro): apply standard CRO checklist to event pages"

git add app/stanwell-pub/page.tsx app/ashford-pub/page.tsx app/staines-pub/page.tsx app/feltham-pub/page.tsx
git commit -m "feat(cro): apply standard CRO checklist to location pages"
```

---

## Final Verification

After all tasks are complete:

```bash
npm run build
```

Expected: successful build with no errors. If build fails, fix TypeScript or import errors before proceeding.

```bash
npm run lint
```

Expected: no linting errors.

Spot-check these pages in the browser (or via `npm run dev`):
- `/` — hero has value prop sub-headline and rating in trust badge
- `/beer-garden` — hero has 2 secondary CTAs (not 3); ReviewsBadge visible below hero; no Smoking Area feature
- `/near-heathrow` — clean "Book a Table" primary CTA; ReviewsBadge section; Book Table in bottom CTA; sticky bar visible
- `/food-menu` — price signal in hero description
- `/sunday-lunch` — deposit tag is green/positive
- `/private-hire/wakes` — phone CTA is primary
